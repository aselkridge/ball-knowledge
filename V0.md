# V0 — what goes to the twenty

**THIS FILE IS THE PLAN. It is the only live plan.**
Locked by Aaron 2026-08-01. **Reconciled against the repo 2026-08-03** — every
tick below was checked against code, `tools/audit.py` or a commit, not recalled. Everything else in the repo is history, reference, or
reasoning. If another document disagrees with this file, **this file wins.**

> Anything in `BUILD.md` § 3 "Build phases" or § 4 "What's next" is the **July 29
> draft board**. It is good work and it is **SUPERSEDED**. Aaron's goal changed on
> 2026-08-01 and that board was never updated. Do not plan from it. Do not blend
> it with this. If you find yourself merging the two lists, stop — you are about
> to rebuild the confusion this file exists to end.

---

## The goal, in Aaron's words

> *"I really need to get this out to that 20 person group and fast... If I cannot
> get adoption from these 20 then what am I even doing. People aren't going to
> want to repeat play and give feedback to a game that is already boring and they
> cannot play with their friends to try and show other people it's fun."*

Every item below earns its place by answering one question: **will this make
twenty people play it twice and show it to someone else?**

## The scope boundary — read this before touching data

**NBA and WNBA only. Complete. Nothing else.**

Aaron, 2026-08-01: *"getting the NBA and WNBA cleaned up and ready is all we need
for now, and that means full data, stats skills everything."*

- **Complete** = full data, stats, roles, current players current, sources tiered,
  answers verified, tags confirmed. Not "exists" — *finished*.
- **Every other league waits.** College, Street, Flags, Overseas, BIG3, G League,
  Early Black Basketball, FIBA 3x3, Wheelchair stay in the tables and stay OUT of
  the packs until their own research lands in a later release.
- **Packs stay switched on**, and serve **only** cards that pass the verified-pack
  gate. The counter tells the truth about how many have earned their place.

---

## The research runs — all 9, NBA + WNBA only

Aaron: *"my data and research I want done first, no question."*
Each number is a gate metric in `tools/audit.py` and can only go down.
Live counts: `python3 tools/todo-build.py` · browse them: `/tape/` → saved views.

> **Re-measured 2026-08-04.** R3 has since CLOSED; everything else is unchanged
> from the day this file was locked. Re-run `python3 tools/todo-build.py` before
> quoting any of these — that command is the record, this table is a copy.

| run | open | what it is |
|---|---|---|
| **R0** | ✅ done | the `todo` table — every gap as a joinable row |
| **R1** | ✅ done 08-04 | card source does not resolve to a fact — was 829. Cards now carry `f`, the fact id; `tables-emit.py` writes it |
| **R2** | 604 | stat row with fewer than five real fields |
| **R3** | ✅ done 08-03 | source with no reliability tier — was 513. `tools/tier-sources.py` + `source_register.json`; spec in TABLES.md |
| **R5** | 401 | card missing an era or player tag |
| **R6** | 98 | volatile card that can go stale |
| **R7** | 1,024 | position/rating that applies to every decade at once |
| **R8** | 574 | accolade with no year |

> ⚠️ **This heading says 9 runs and the table lists 8.** There is no R4 anywhere
> in the repo — it was never written, not lost. Left visible rather than quietly
> renumbered, because renumbering a plan mid-build is how two sessions end up
> talking about different R5s. Same class of error as the build list below, which
> says 23 and enumerates 22.

## GATE 1 HAS TWO HALVES NOW — RULED by Aaron, 2026-08-07

> *"is that 1000 wide enough or is it 1000 who was the mvp questions... maybe
> 1000 total questions isn't the request, maybe the request is build as many
> questions as possible, but make sure we have at least 1000 that can cover 20
> people playing 20 times each and getting varying questions."*

**He is right that a raw total is the wrong target, and the arithmetic backs
him.** 1,000 says nothing about what a player experiences. Two banks of 1,000
can feel completely different: one where every card is a Finals MVP question,
and one spread across leagues, eras, difficulties and question shapes.

**The number that matters is per-player exposure, not bank size.** Twenty people
playing twenty games each is not 400 cards, it is twenty separate experiences of
about twenty games. What each of them feels is how often something comes back.
That depends on three things the total does not capture:
- **how many cards a game actually deals** (needs measuring from the real game,
  not estimated),
- **how the pool is SLICED** — a player who picks NBA + 1990s + hard is not
  drawing from 1,000, they are drawing from whatever that intersection holds,
  which today is a small fraction,
- **rotation** — see BUILD.md § 6 · 22ag, filed the same day.

**The likely reframe, for Aaron to rule on:**
1. **Keep 1,000 as a floor, not a target.** It stays the launch gate.
2. **Add a SPREAD requirement**: a minimum per league x era x difficulty cell,
   so the thousand cannot all be one kind of question. The verified-pack gate
   already does exactly this shape at 25 per league x difficulty; this extends
   it to eras and question shape.
3. **Uncap the ceiling.** The real instruction is *build as many as possible*,
   and every question banked beyond the floor improves rotation forever.
4. **Measure the actual experience** before picking numbers: deal 20 simulated
   games per player profile through the real picker and count repeats.
   `gate-check.mjs` already drives the real picker across every league x decade
   x tier, so the harness exists.

### AARON'S RULING, same day

> *"Basically I want all of the questions and facts, but small goal before the
> launch to the 20 friends is that the question pool of 1000 is at least pretty
> diverse."*

So Gate 1 is **1,000 dealable cards AND a spread that stops them all being one
kind of question**. The ceiling is uncapped: build every question and fact we
can, forever. The 1,000 is a launch floor, not the ambition.

### WHAT "PRETTY DIVERSE" MEANS, MEASURED NOT INVENTED

`python3 tools/diversity.py` is the check. Its bars were set by measuring the
318-card pool on 2026-08-07 and asking, per dimension, *is this already fine, or
is this the thing that would make twenty games feel repetitive?* They scale with
the pool, so the question is answerable today rather than only at 1,000.

**Where the pool stands right now: 7 of 8 dimensions pass.**
*Re-measured 2026-08-07 after the era tagging pass and the `all-eras` tag.
Era tagging has gone from the worst dimension to a passing one; history is
still the gap, and it is now a WRITING job rather than a tagging one.*

| | state 2026-08-07 |
|---|---|
| difficulty spread | **PASS** — 40 / 76 / 84 / 68 / 50 across the five tiers, thinnest is 48% of the fattest |
| category breadth | **PASS** — 77 categories |
| subject concentration | **PASS** — Jordan is the biggest at 12 cards, 6% of person-tags |
| breadth of subjects | **PASS** — 95 distinct people across 318 cards |
| top 10 people | **PASS at 34%**, but the bar tightens to 25% at 1,000 |
| league x difficulty | **PASS** — this is the verified-pack gate, already enforced |
| era tagged | **PASS — 14%** of dealable cards untagged, down from 37% (bar: 20%). 42 more are explicitly `all-eras`, which is correct and must be left alone |
| **every era present** | **THIN — 6 eras below the floor**: 1930s, 1950s, 1960s, 1970s, nba-1940s, nba-1950s. *(1890s cleared: 1 card to 16.)* **This is the one remaining gap and Track A · A5 is aimed straight at it.** |

**So diversity is not the problem people would assume.** The bank is not a pile
of MVP questions: difficulty, category and subject are all healthy. **The one
real gap is history** — the pool skews modern. As of 08-07 that is no longer a
tagging problem, it is a supply problem: there are not enough pre-1980 cards to
tag. Writing them is Track A · A5, and the season spine already holds the facts.

### WHAT THAT CHANGES ABOUT THE WORK

1. **Era tagging is a launch item, not a nice-to-have.** ~~117~~ **43** dealable
   cards still have no era (321 across the whole bank of 1,526), down from 117
   after the 08-07 pass. Q6's territory, and cheap: no research, only tagging.
   Track A · A3.
2. **New questions should be aimed, not random.** `diversity.py --thin` prints
   the worst gap first. Pre-1980 basketball is where writing effort belongs.
3. **The Black Fives era, early pro and the 1950s-70s are underweight** in the
   dealable pool, which is exactly the coverage Aaron cares most about
   (BUILD.md § 5b.1) and exactly what the marginal-history research runs feed.

### STILL OWED

The real measure is **repeats per player over twenty games**, not a spread
report. That needs the actual picker driven over simulated sessions;
`gate-check.mjs` already drives it across every league x decade x tier, so the
harness exists and the job is to add session simulation. Until it runs, any
claim about what twenty games FEEL like is a guess, and `diversity.py` says so
about itself.

## LAUNCH BAR — Aaron's rulings, 2026-08-06

Two hard gates. **Neither alone is enough**, and Claude was told to stop
proposing launch before both are met.

**GATE 1 · 1,000 verified NBA/WNBA cards — and that means WRITING NEW ONES.**
Aaron: *"for the 1000 questions, that is for launch so thats NBA and WNBA only."*
Measured 08-06 and the arithmetic does not work on existing cards alone:

| | |
|---|---|
| in-scope cards that EXIST in the whole bank | **961** |
| shipping now | ~~306~~ **317** *(08-07)* |
| target | 1,000 |

**Re-measured 2026-08-07 with `tools/gate-blockers.py`, and it is sharper than
the 961 above.** Not every in-scope card is *reachable*: 317 source rows carry
no url at all and are a finding job, not a reading one.

```
 317   dealable and in scope today
 607   ceiling if EVERY readable card gets verified
 393   cards that must therefore be NEWLY WRITTEN
```

**So verification is the road to 607, not to 1,000.** Any plan that says
"finish verifying, then start writing" is a dead end with a number attached.
This is why writing new cards sits at A5 in the order below rather than at the
end, and it confirms Aaron's 08-06 ruling — keep 1,000, write the gap — with a
bigger gap than the 150-200 estimated then.

Even verifying every in-scope card lands at 961, and some will be quarantined,
so realistically ~860. **Aaron's ruling: option 1 — keep 1,000 and write the
gap.** Roughly 150-200 new NBA/WNBA questions on top of verifying nearly
everything that exists.
The two jobs feed each other, which Aaron flagged as the key insight: mining a
Basketball-Reference leaderboard to fix sourcing on 30 existing cards also hands
over ~20 new question ideas **with a Tier 1 source already attached**. Write the
gap alongside the sourcing work, never as a separate project.

**GATE 2 · the 27 build items.** Aaron: *"I am not launching to the 20 until I
get to where I said I want to be for them. That includes the home screen app and
so much more."* Status checked against the code 08-06 — see below: 10 done,
1 part-done, 16 not started.
**And they start the moment the card target is hit, running ALONGSIDE the
research, not after it.** Aaron: *"Once we get to that 1000 we HAVE to start
building the things in my list ASAP so maybe even during the research runs."*

**SEQUENCING · V29 goes first.** Aaron: *"maybe v29 is our next run so we don't
waste time on research only to find out we can't use it."* Correct, and it
overrides Claude's earlier suggestion of running the census first. The licensing
answer changes what the other runs are even allowed to do.

**ALSO ORDERED 08-06, ahead of research:** the coming-soon page, then a rebuilt
all-inclusive status board.

### DAILY FIVE · Aaron's playthrough, 2026-08-07 — eight items

Found by playing it. Not scheduled; filed the turn they were raised. **Three are
cheap bugs with diagnoses attached, one is a copy fix, and four are the same
underlying problem wearing four hats.**

#### The cheap ones

- [ ] **D40 · THE DAILY FIVE STARTS WITHOUT ASKING.** Aaron, 08-18: *"can you
  add a confirm or let's go button for the player to press before starting
  the offense defense and bonus?"* Three doors, one button each: before round
  one deals, before Defend the Floor deals, and before the Heat Check clock
  starts (the bonus already has "Unlock the Heat Check", so the question
  there is whether THAT press should also be the ready check or whether the
  clue should wait for one more tap; his wording says one more tap). Nothing
  should put a clock or a card in a player's face before they said go.

- [ ] **D41 · PLAYING OLDER DATES IS INVISIBLE AFTER TODAY IS DONE.** Aaron,
  08-18: *"can we make it more clear how the player can play more dates after
  beating the current day? Maybe make it so if they click the icon on the
  main menu after it was already completed the calendar pops up
  automatically?"* His own suggestion is the spec: a finished today means the
  menu stamp opens the CALENDAR first, receipt one tap behind it, instead of
  landing on the receipt with the streak button below the fold.

- [ ] **D42 · LEAVING AN OLD DATE MID-RUN THROWS THE RUN AWAY.** Aaron,
  08-18, and it is a bug with a precise spec attached: *"if you leave a daily
  5 from an older date mid daily 5 then it just goes back to the beginning,
  that shouldn't happen, it should return to the game/date they were in and
  they miss that question just like if it were the current day. But be sure
  that when they finish that doesn't mean that when they return to the daily
  5 that the old date end screen should show, it should still be the calendar
  over the current date."* Two halves: (1) the resume-with-a-miss rule that
  TODAY already has must apply to any date being played (the run save is
  keyed to today only, `loadRun`/`saveRun`); (2) finishing an old date must
  NOT make it the screen the mode opens on later, reopening always lands on
  the calendar over the CURRENT day. Watch the interaction with D41, they
  share the landing rule.

- [ ] **D43 · THE MENU SLAM FIRES OFF-TILE, REOPENED, and scrolling triggers
  it.** Aaron, 08-18: *"the main menu issue is still there where if you click
  a vs tile the slam thing shows up somewhere else on the screen, also just
  touching to scroll in the peaking Rolodex causes the slam words to show up
  it's weird."* Two defects in one surface: the SOON/VS slam still positions
  wrong on a phone (the 08-16 fix did not hold on his device, so whatever it
  anchored to is not the tile's real on-screen box on iOS Safari), and a
  SCROLL gesture on the rolodex counts as a tap, so browsing fires slams. The
  second half needs a drag threshold: a touch that moves is a scroll, not a
  press. Reproduce on a 390px touch viewport with a real drag gesture, not a
  click, or it will pass again while staying broken on his phone.

- [x] ~~**D1 · The answer stays highlighted on the next question.**~~ **FIXED 2026-08-07.**
  `.dva:hover` is now inside `@media (hover:hover)`, so it only applies to
  devices with a real pointer. Diagnosis held: `showCard()` rebuilds the buttons
  with `innerHTML` + `createElement`, so `.right`/`.wrong` could never survive —
  it was sticky hover on the finger, on a button created at the same position.

- [ ] **D1c · A CANVAS ELLIPSE CAN BE ASKED FOR A NEGATIVE RADIUS.** Filed
  2026-08-17, found by the toll probe: teleporting pieces mid-render made a
  draw call throw `IndexSizeError` (major-axis radius -2.7). Never seen in any
  real-play harness, and they all assert zero page errors and pass, so this is
  a landmine for FUTURE harnesses and edge states more than a player bug. The
  fix is a `Math.max(0, r)` at the ellipse call sites in `game.js` (five of
  them, lines near 2342-2477), next time someone is in that file.

- [ ] **D1b · THE OTHER 42 HOVER RULES HAVE THE SAME BUG.** Type C. Filed 2026-08-07.
  Counted, not estimated: **43 unguarded `:hover` rules in `index.html`, and
  every one is on a tappable control** — the daily stamp, the menu buttons, the
  pack chips, the court tiles, the rulebook controls, the calendar nav. Same
  class of bug, same one-line fix each.
  **Deliberately NOT swept in the D1 commit, and the reason is the interesting
  part.** The mechanical transform was written and run twice; both times it
  double- and triple-wrapped rules (`@media (hover:hover){@media (hover:hover){`)
  because the skip branch was wrong, and the second attempt silently dropped
  content between matches. It was reverted both times.
  **A 43-rule CSS rewrite nobody can eyeball is not a fix, it is a new risk**,
  and it belongs in its own commit with its own before/after at desktop AND
  touch — hover styles must still work on a laptop, which is the half a naive
  sweep breaks. Do it deliberately, nine multi-line rules by hand.

- [x] ~~**D3 · THE DAILY FIVE HAS NO SOUND AT ALL.**~~ **FIXED 2026-08-07.**
  Confirmed: zero `BKAudio` calls, and nothing needed sourcing. Wired through
  one `sfx()` wrapper so the mode's whole voice reads in eight lines and the
  settings toggle keeps working for free: tap → `select`, right → `net` (the
  swish), wrong → `brick`, **out of time → `buzzer`, which is its own sound
  because running out of time is a different feeling from choosing wrong**,
  round change → `whistle`, perfect ten → `horn`.
  `tools/daily-sfx-check.mjs`, 11 checks, three sabotages proven. It records
  what the game ASKS FOR rather than listening for audio, because whether a
  swish sounds like a swish is Aaron's ear and not a harness's job.

- [x] ~~**D3 (original) · THE DAILY FIVE HAS NO SOUND AT ALL.**~~ **FIXED,
  B5b (see the current D3 entry below) · mark flipped 08-11.** Aaron:
  *"when you click an answer on the daily 5, when you get to the next answer
  that one is still highlighted."*
  **DIAGNOSED, and it is not a state bug.** `showCard()` rebuilds the buttons
  from scratch (`innerHTML=` then `createElement`), so `.right`/`.wrong` cannot
  survive. The culprit is `index.html:538` — `.dva:hover:not(:disabled)` — and
  **sticky hover on touch**: a phone keeps `:hover` on the last-tapped element
  until you tap somewhere else, and the next card's button is created at the
  same screen position under the same finger.
  **Fix is one line:** wrap that rule in `@media (hover:hover){...}` so it only
  applies to devices with a real pointer. Worth grepping for other `:hover`
  rules on tappable things in the same pass, because this is a class of bug,
  not an instance.

- [x] ~~**D2 · "I'll be back" promises something the game does not do.**~~ **FIXED 2026-08-07**, and it turned into a voice pass rather than a string edit.
  Aaron spotted WHY it was wrong, not just that it was: *"when you make a shot
  it says something along the lines of both getting it right and making a shot
  right? So can we do opposite things for getting it wrong/bricking it?"*
  Exactly. The makes are pure basketball outcome — *Wet, Splash, Denied, Wall* —
  and the shot going in IS the "you got it right". The misses started in that
  voice and then switched to the game talking about scheduling, which was both a
  second voice and a promise the mode cannot keep.
  **Every line the Daily Five speaks now lives in ONE block** (`LINES` in
  `daily.js`), because four ternaries across two functions is how the voice
  drifted without anyone noticing.
  **Three things fixed beyond the reported one:** misses had two lines against
  the makes' four, so repeats showed twice as fast exactly when you notice most —
  now four of everything. Out-of-time had a single fixed string per round and no
  rotation — now four each, and in *clock* language rather than wrong-answer
  language, because being caught by the shot clock is a different feeling and
  already has its own buzzer. And the bonus round got the same treatment: heat
  language for the hit, the opposite for the miss, its own set for the clock,
  picked by DATE rather than card index because the bonus fires once a day.
  Guarded by six checks in `tools/daily-sfx-check.mjs`, including one that fails
  if any line ever promises a card will return. Aaron:
  *"when you get an answer wrong it says I'll be back but we aren't doing that
  for daily 5 remember because it ruins the everybody gets the same questions."*
  He is right, and **`daily.js` already knows** — the comment above `answer()`
  says in bold that the come-back mechanism was never built and warns against
  re-adding the claim without it. The copy is at `daily.js:640-648`, six
  strings.
  **The design answer is to change the copy, not to build the mechanism.** The
  daily is seeded from the DATE alone so everyone gets the same ten; a
  personalised "your missed cards return" set is the exact thing that breaks
  "did you get today's?", which is the only reason a daily exists.
  So the line needs to do the job "I'll be back" was doing — sting a little
  without teaching the answer — with no promise in it. Aaron's ear, not mine.

- [x] ~~**D3 · THE DAILY FIVE HAS NO SOUND AT ALL.**~~ **FIXED in B5b** —
  `daily.js` now carries 3 `BKAudio` call sites (verified 08-11: net, brick,
  buzzer + whistle and horn through `sfx()`), guarded by
  `tools/daily-sfx-check.mjs`, 11 checks. Mark flipped 08-11; the B5b strike
  in the Track B table was done the same day the fix shipped, this copy of
  the item was missed. ~~Old text:~~ Aaron asked whether we have
  quick sounds and whether he needs to source them. **Measured: `daily.js`
  contains ZERO `BKAudio` calls.** Not the wrong sounds — no sounds.
  **AND HE DOES NOT NEED TO SOURCE ANYTHING.** `audio.js` already synthesises
  every sound he described, in code, no files: `net` (an arpeggio that reads as
  a swish), `brick` (a noise hit), `buzzer` (a falling sweep, which is the
  shot-clock sound he asked about), plus `whistle`, `horn`, `zap`, `click`.
  The job is wiring, not sourcing: right → `net`, wrong → `brick`, timeout →
  `buzzer`, tap → `click`, and the sweep/receipt gets `horn`.
  **Do this one first.** It is the cheapest item on the list by a distance and
  it feeds D4 below — half of "more POP" is that nothing currently makes a
  noise when you win or lose.

#### The four that are one job

Aaron listed these separately and they share a root: **the Daily Five reports
results, it does not stage them.** A caption fading in and out is a status
message. Winning a point should be an EVENT.

#### THE THREE ENDINGS — Aaron's requirement for the revamp, 2026-08-07

> *"we want a really cool ending pop for people who get all of them right and
> even more for those who get the bonus, and even if you finish the regular
> daily five there should be at least a crowd cheer at the end or something."*

**Three tiers, and the point is that they are visibly different from each
other**, so finishing feels like something and a sweep feels like a lot more:

| | what you did | what you get |
|---|---|---|
| **FINISHED** | played all ten, any score | a crowd cheer and a receipt worth looking at. Nobody leaves in silence. |

##### THE SOUND SHOPPING LIST, and where to get it (Aaron asked 2026-08-07, answered 08-08)

**What the game has today, measured, not remembered.** `audio.js` synthesises
**eleven** effects from oscillators and noise: `click select net buzzer brick
steal tap whistle whoosh horn zap`. There are **zero** sound-effect FILES in the
repo. The only sourced audio is the soundtrack: eight Ketsa mp3s in
`docs/play/audio/`, CC BY 4.0, credited in the Rulebook.

**The line between synth and sourced is not taste, it is physics.** An
oscillator can fake a buzzer, a whistle and a rim ping, because those are simple
tones. It cannot fake anything made of MANY THINGS AT ONCE: a crowd, a shoe on
wood, a net, a ball on hardwood. Synthesised crowd noise sounds like a
hairdryer. Every item below is on the sourced side for that reason.

| Sound | Why it cannot be synthesised | Notes for the search |
|---|---|---|
| **Crowd cheer** (the one Aaron asked for) | hundreds of voices, no periodicity | 2 to 4s arena swell, **no music, no commentator**, clean tail. **Ideally 3 intensities** so a sweep can be louder than a finish |
| **Crowd groan / ooh** | same | 1 to 2s, for a miss on a big moment |
| **Sneaker squeak** | broadband stick-slip | short, dry, 2 or 3 variants so it does not loop-sound |
| **Ball on hardwood** | transient plus room tone | a dribble and a single bounce, separate |
| **Net swish (real)** | dozens of nylon strands | the synth `net` is fine for the Daily Five; a real one would lift a made bucket |
| **Rim clank** | metal ring with a long decay | pairs with the brick |
| ~~**FOOTSTEPS**~~ **IN THE REPO** (`steps-hallway.mp3` 10.9s, `steps-concrete.mp3` 60s walking loop, both measured; sliced 08-09, and the audition verdict was mixed: concrete cuts fine, hallway cuts MISSED the footfalls — see THE AUDITION CAME BACK below) *(added 08-09, Aaron: "ima need to source the footsteps")* | a footfall is a whole body landing on a floor, and the floor is half the sound | **The single biggest thing in THE PLACES spike**, bigger than the head bob. Want **two surfaces**: wood for the gym, blacktop/concrete for outside. 3 or 4 variants each so a walk does not tick like a metronome. Slow walking pace, one foot per file, dry, no reverb, so the room can be added. The spike synthesises these today (filtered noise, fast decay) and it is audibly a placeholder |

#### THE AUDITION CAME BACK, and it changed the method · 2026-08-09, night

Aaron listened to the 51 slice candidates. His verdicts, exact words where
they carry the ruling:

- **A ball drop is ONE sound and is never sliced.** *"the ball drop for both a
  and b is one whole sound, the sound of a basketball bouncing with less time
  in between each bounce after it is dropped to the floor."* A drop is a single
  gesture whose bounce intervals decay geometrically; cutting it into "bounces"
  destroys the thing itself. **RULED: drops ship whole.**
- **Bounces: all keepers** (*"I like all of them"*). **Swishes: all keepers**,
  and #5 is a backboard-then-swish variant, *"nice variety"* — wired into the
  B5c sample as the 1-in-4 bank-shot window.
- **Rim hits 1, 3 and 5 cut the ring short** (*"so I cant really vote"*): my
  end-detection gated the quiet metallic tail as if it were room noise.
- **Hallway steps: the slicer missed the footfalls entirely**
  (*"you missed the step sound alltogether somewhow"*).
- **Concrete steps: "they are all just steps"** — the consistency pick is mine
  to make, 3 or 4 by matching level and length.

**What that ruled in, method-wise: WINDOWS, not cut files.** The B5c sample now
plays [offset, duration] windows into the ORIGINAL mp3s through WebAudio, with
gain-ramped tails as long as we want — which is the direct answer to the
cut-short rim ring: a window's tail costs nothing to extend. No cut files ship;
`manifest.json`'s measured numbers ARE the sound bank. A re-cut audition (rim
with full tails, hallway re-gated) is owed ONLY if standalone one-shot FILES
are ever needed; for the game, windows supersede slicing. **FILED as that
conditional, here.**

- **The synth whistle failed his ear, and he fixed it by sourcing**: *"that
  sound before defense doesnt sound like a whistel at all, so I added some to
  this folder."* Two files pulled and measured the same night, so the repo now
  holds **nineteen** sfx: `whistle-coach.mp3` (5.9s, two blasts at 0.15s and
  0.80s; the sample's round-2 whistle plays the window off 0.13 dur 1.3) and
  `whistle-ref-kit.mp3` (22.5s, **13 blasts**, a whole kit of variants for the
  real build to rotate through).
- ***"There were no cheer sounds in the sample"* — TRUE, and the bug taught
  the day's best lesson.** The cheers were loaded with `fetch()` of a `data:`
  URI, which works on `file://` where the check harness runs and is silently
  blocked by the artifact page's CSP. The harness certified the wrong
  environment. **FIXED**: one decode path (`atob` → `decodeAudioData`) that
  exists in both environments, and the harness now fails on any `fetch(` in
  any script — sabotage-proven. Cross-filed to `AI-LEARNINGS.md`.
- **The comparison he asked for** (*"I would love to hear a comparison of the
  arcade sounds in the daily 5 and the sounds I gave you"*) is live in the B5c
  sample: a **Sounds: YOURS / ARCADE** toggle in the control rack, default
  YOURS. YOURS = real swish (bank shot 1 in 4), real rim clank plus a real
  bounce on the miss, real coach whistle before defense, real cheers on all
  three endings. ARCADE = the synth recipes, note for note. 61 checks green on
  both viewports.

#### THE COMPARISON CAME BACK: real sounds WIN · 2026-08-09, later

- **RULED, Aaron:** *"For the most part Daily 5 is great! and i do not want
  the arcade sounds, the real sounds are MUCH better!"* **The real files are
  the Daily Five's voice. The arcade synth ships nowhere in it** (it can stay
  in the code as the recipe library for UI blips, but no game moment plays a
  synth where a sourced file exists). The real B5c build wires the real
  windows only; no toggle.
- **FILED, the crowd is not settled:** *"the plain Finished crowd isnt even
  cheering and it ends on this weird PA announcement getting cut off, lets try
  a different crowd sound... feel free to give me a set to listen to for each
  ending and Ill pick."* He is right twice, and the second one is measurable:
  the FINISHED window pointed at `crowd-cheer-reacting` from 0.05s because the
  lead-silence number said sound starts there — but **sound is not cheering**,
  and `tools/crowd-swells.mjs` (new) shows that file's actual swells sit at
  **6.5s and 13.3s**. Picking a window by silence numbers alone was the same
  mistake as cataloguing sounds by filename (AI-LEARNINGS 2.6v): an adjacent
  measurement standing in for the property that mattered.
- **The set he asked for is built**: every crowd file we own scanned for real
  swells (RMS envelope over a measured bed; numbers committed in
  `sfx/crowd-swells.json`), nine candidates, each playable through **its
  ending's exact in-game treatment** (3.5s/0.5 · 4.5s/0.75 · 6.5s/1.0, the
  realPlay fade), pick one per ending, copy-verdicts button.
  `crowd-bed-whistles` left off on purpose: measured flat, +1.6 dB, ambience
  not cheer. 25 checks green, sabotage-proven.
  <https://claude.ai/code/artifact/5a2b6327-1634-4b31-8e8f-34e5e0a8660f>
- **AND HE PICKED, same night — RULED and WIRED into the sample:**
  - **FINISHED**: *"live game crowd as you had it but extend it to 5s"* → the
    game bed's late swell, `crowd-bed-pa` window off 102.5, dur **5.0**, gain
    0.5. (Read as the LATE SWELL, the clean one; if he meant the loud open,
    it is one number to change.)
  - **SWEPT**: *"the big roar from its rise but make it same length as roof
    off"* → `crowd-cheer` off 0.85, dur **6.5**, gain 0.75.
  - **ROOF OFF**: *"the big roar, already rolling, combined with the announcer
    game crowd, big moment?! you probably have to raise the volume of
    announcer"* → LAYERED: `crowd-cheer` off 3.5 dur 6.5 gain 1.0 **+**
    `crowd-bed-squeaks` off 14.2 dur 6.5 gain **1.25** (the boost).
  These windows are the real build's cheer bank; they live in the sample's
  REAL registry and `sfx/crowd-swells.json` holds the measurements.

#### Two more Aaron questions, same evening, both FILED awaiting his ruling

- **"the defense is just 5 squares, anything more creative?"** True in the
  shipped Daily Five and in the sample: round 2 was a strip of five tiles.
  Options presented 08-09 (defend-the-floor / the wall / the standoff); Aaron,
  same night: *"I also like the Defend the floor option."* **RULED and BUILT
  into the B5c sample**: round 2 keeps the COURT, flipped to the defense tint,
  the five STOPS (same names, points, tiers) sit tight to the rim as the
  opponent's attack spots, and the ball flies IN — right = it dies at the iron
  and caroms out under a teal DENIED, wrong = their swish, dry, cold stamp,
  red X. The wall and the standoff are dead. Colour law held and
  harness-enforced (3 tier colours on the spots, sabotage-proven). Before/after
  comparison, both widths:
  <https://claude.ai/code/artifact/140ddc6a-2c3c-4f84-bedc-fb6705ccc595>
  The real build (B5) carries this to `daily.js`; spot positions are staged
  guesses Aaron may still retune.
- **"what are the sounds when right or wrong for defense, and for heat
  check?" MEASURED** (`daily.js:792` and `:1142`): both rounds and the heat
  check play the SAME three sounds — right = `net`, wrong = `brick`, timeout
  = `buzzer`. No defense voice exists. Which means **a successful STOP is
  rewarded with the sound of a made basket**: the wrong fiction, same class
  of collision as red-means-hard vs red-means-three. Proposal (08-09):
  defense right = the opponent's shot CLANKING off the real rim (+ crowd
  swell), defense wrong = their swish, dry, crowd silent; heat check hit =
  real swish + a crowd layer that grows per consecutive hit. **RULED, same
  night, after hearing it staged in the sample: "Defend the floor and the
  sounds in daily 5 are perfect! You got it!"** The staged defense voice is
  the spec for the real B5 build. The heat-check growing-crowd layer is still
  only a proposal on paper (never staged); it rides the real build and gets
  his ear there.

#### AND NOW THEY ARE IN THE REPO · 2026-08-09, evening

Aaron renamed every file by ROLE (*"here is the sound file, named correctly"*),
and all seventeen were pulled straight from the Drive folder into
**`docs/play/assets/sfx/`**, byte-for-byte verified against the listing.
Two sidecars ride with them:

- **`provenance.json`** — Aaron's renames lost the original Pixabay filenames,
  so the licence trail was rebuilt by exact byte size before it could rot. Every
  file maps back to its `uploader-title-id` original. **Licence CONFIRMED
  2026-08-09**: asked whether everything came from Pixabay, Aaron: *"Yes."*
  (The two whistles he added later the same night carry no ID3 tag, so their
  provenance rows say exactly that instead of inventing a source.)
- **`manifest.json`** — written by **`tools/sfx-measure.mjs`**, which decodes
  every file in Chromium's own WebAudio (the decoder the game uses) and records
  duration, peak dBFS, RMS dBFS, and leading/trailing silence. **I cannot hear;
  I can measure.** Whether they sound GOOD stays Aaron's call; whether they are
  usable one-shots is now arithmetic.

**What the measurements found, verdicts attached:**
- **The two cheers have measured roles**: `crowd-cheer` is the loud one
  (RMS −15.1 dB) and `crowd-cheer-reacting` the politer (−18.7), which is
  exactly the two-intensity family the endings spec asked for. **WIRED** into
  the B5c sample's three endings, windowed and gain-staged (0.5 / 0.75 / 1.0).
- **`crowd-cheer` opens with 804ms of silence and trails 2.2s**; the buzzer
  hides **647ms** before it buzzes; the swish leads 261ms. Played raw these
  feel broken. **HANDLED**: the manifest's numbers are the playback offsets,
  and the real build reads them rather than trusting file starts.
- **`time-machine-takeoff` is clipped** (peak +0.8 dBFS after decode).
  **FILED**: fine for a spike, should be re-sourced or peak-limited before it
  ships in the career.
- **`rim-hits` peaks at exactly 0.0 dBFS** and holds ~7s of multiple hits;
  the bounce/drop files are multi-hit too. ~~FILED~~ **SLICED, same evening**
  (Aaron: *"idk if you can cut the footsteps and the basketball bouncing
  correctly either"*): `tools/sfx-slice.mjs` found **220 hits** across the
  seven multi-hit files by onset detection over a measured noise floor and cut
  **51 one-shot candidates**, 3ms/25ms fades so no edge clicks. **The cutting
  is arithmetic; the CHOOSING is Aaron's**, so nothing shipped: every candidate
  is on an audition page with a play button, its waveform, Keep/Kill, a
  play-them-in-a-row gait test, and a copy-verdicts button. Keepers get
  committed, the rest die.
  <https://claude.ai/code/artifact/6897b7dd-35c2-413e-99ac-8edb5d034f77>

**Three corrections to the morning catalogue, since it is now superseded:**
- **The rim clank was NEVER missing.** `basketball-85872` was the rim all
  along; Aaron's rename (*Basketball Hitting Rim Sounds*) identified what my
  filename-reading could not. Only the **sneaker squeak** still lacks a clean
  one-shot, and even that lives inside `crowd-bed-squeaks` and can be sliced.
- **Aaron deleted the byte-identical duplicate** (`basketball-96777 (1)`), so
  the folder is seventeen files, not eighteen.
- **The three mystery sounds are not mysteries: Aaron has sound-designed THE
  PLACES.** *Time Machine Takes Off* (the riser), *Time Machine Powers Up*,
  and *A Gear Turning Sound (picking eras in time machine)*. The career's
  time machine has its audio identity before it has a single pixel. Cross-filed
  to THE PLACES section.

#### The morning catalogue, superseded by the above · 2026-08-09, 18 files

<https://drive.google.com/drive/u/0/folders/12MOdIVkyDaSEtUEFyvthK2CjK8C5WE4N>

**Catalogued by name and size, NOT listened to.** I cannot play audio, so
nothing below is a judgement on how any of them SOUND. That pass happens when
they land in the repo. Every filename follows the `uploader-title-id.mp3`
pattern that Pixabay downloads use, which is the no-attribution source I
recommended, but **Aaron should confirm that before any of it ships.**

| what it covers | files | note |
|---|---|---|
| **Footsteps** | `footsteps-in-a-hallway-47842` (217 KB) · `concrete-footsteps-6752` (1.9 MB) | **Exactly the two surfaces asked for**: indoor for the gym, concrete for the blacktop. Both need slicing into single footfalls; the 1.9 MB one is a walking loop, not a one-shot |
| **Crowd cheer** | `crowd-cheering-379666` (520 KB) · `live-football-match-stadium-crowd-cheering-563439` (559 KB) | Two candidates, pick one. The football one may carry chanting, which the brief said to avoid |
| **Crowd groan** | `crowd-disappointment-reaction-352718` (99 KB) | The "ooh" for a miss on a big moment |
| **Crowd bed** | `fans-at-basketball-game-crowd-5859` (2.2 MB) | Long, so it is an ambience LOOP rather than a one-shot. A bed under live play, which the shopping list never asked for and which is a real upgrade |
| **Ball** | six `basketball-*` files (193 KB to 828 KB) plus `ball-basketball-drop-77956` | Dribbles, bounces and game ambience, unsorted. **Two are the same file**: `basketball-96777.mp3` and `basketball-96777 (1).mp3` are both 193,440 bytes |
| **Net swish** | `basketball-net-swish-sound-40170` (120 KB) | Replaces the synthesised `net` |
| **Buzzer** | `buzzer-or-wrong-answer-20582` (56 KB) | A real one to sit beside the synthesised buzzer |
| **UI / stingers** | `techno-riser-398645` (323 KB) · `futuristic-sound-96179` (87 KB) | A riser is a genuinely good find and nothing asked for it: it fits the toss-up 5-4-3-2-1 and the heat bar filling |
| **Unclear** | `gear_noise-437371` (163 KB) | No idea what this is for. Aaron's call |

**STILL MISSING, both named in the original list:**
- **Sneaker squeak.** Nothing in the folder covers it, and it is the single most
  basketball sound there is. Search *sneaker squeak*, *basketball shoe squeak*.
- **Rim clank.** Pairs with the brick. Search *basketball rim hit*, *rim clank*.

**What happens next, and it needs one thing from Aaron:** get the files into
`docs/play/assets/sfx/` (no CDNs, everything self-hosted) and I trim, normalise,
convert and wire them. Until then they cannot be listened to, sliced or judged.

**Where to get them, in the order I would try.** The rule is the same one this
project already applies to facts: **prefer the licence you can quote.**

1. **Pixabay Sound Effects** (`pixabay.com/sound-effects/`). Free, **no
   attribution required**, commercial use allowed under their content licence.
   Start here because it is the only free source with no string attached to
   the file. Search: *arena crowd cheer*, *basketball squeak*, *sneaker squeak*.
2. **Freesound** (`freesound.org`). Enormous and high quality, but the licence
   is **per file**: filter to **CC0** and nothing else, or you inherit an
   attribution obligation on a game you may sell. Search: *crowd cheer indoor*,
   *gymnasium ambience*, *basketball bounce wood*.
3. **Zapsplat** (`zapsplat.com`). Free with attribution, or a small yearly fee
   to drop it. Good, consistent sports library, which matters: six sounds
   recorded by one person in one room sit together better than six from six
   sources.
4. **PAID, if the free ones sound thin.** `A Sound Effect`, `Boom Library`,
   `Soundsnap`. One-off purchase, written commercial licence, no attribution.
   A basketball pack is usually 20 to 60 dollars and would cover this whole
   table in one go. **This is the option I would actually recommend** if the
   game is ever going to take money, because it turns six licence questions
   into one receipt.

**AVOID:** the BBC Sound Effects library (free, but the licence is personal and
educational, NOT commercial, and this game may go commercial), YouTube rips, and
anything on a "free sounds" aggregator that does not name a licence on the page.
A sound with no licence is the audio version of a fact with no source.

**What Aaron actually has to do:** download, drop the files in
`docs/play/audio/sfx/`, and paste the source URL and licence next to each one.
I will wire the tiers, the ducking against the music, and the volume slider.
**Record the licence in `PLACES.md` the same way Ketsa is recorded** or it will
be unanswerable in six months.

**Format:** mp3 at 128kbps mono is plenty for effects and keeps the install
small. Keep the originals somewhere; we can always re-encode down, never up.
| **SWEPT** | 10 of 10 | the big one. This is the moment the mode exists for. |
| **SWEPT + BONUS** | 10/10 and the Heat Check | more again, and it should feel like the roof coming off. |

**MEDIUM CHECK, and it splits — this is not one job.**
- **The visual half is VECTOR/CSS/canvas: build it.** Confetti, a rising score
  counter, the rack lighting up shot by shot, a stamp slamming down, the arena
  brightening. All geometry and type. `heat.js`'s ON FIRE slam already proves
  the game can do a big moment well, and DESIGN § 9 owns the look.
- **THE CROWD CHEER IS THE ONE THING THAT MUST BE SOURCED.** `audio.js`
  synthesises everything it plays — `net`, `brick`, `buzzer`, `whistle`, `horn`
  are all oscillators and noise. **A crowd is not synthesisable at any quality
  worth shipping**, and a fake one will sound worse than silence next to real
  arena art. So this needs a real recording: an arena crowd swell, roughly 2 to
  4 seconds, no music, no commentator, clean tail so it can duck under the
  receipt. Two or three intensities if possible (polite / loud / roof off) so
  the three tiers can use the same family. **Aaron sources it; I cannot make it
  and should not pretend.** File it under the same standard as the music that is
  already in the game.

**AND THE DESIGN GOALS THIS HAS TO MEET**, since Aaron asked for them to be
remembered: it is his game's own look (DESIGN § 9 — arena, warm near-black,
arena orange, Anton, DSEG7), it reuses devices the game already has rather than
inventing new ones (CLAUDE.md's third option), and it ships with a before/after
comparison because it is a visual change.

**THE SAMPLE IS BUILT, 2026-08-09** (Aaron: *"Let's see your sample of B5c"*):
`docs/dev/daily-theatre.html`, playable at
<https://claude.ai/code/artifact/efc4fa3d-e5ac-4184-9bf8-34f9f1aa3809>, built by
`tools/theatre-sample-build.py`, 45 checks in `tools/theatre-sample-check.mjs`,
three sabotages proved (flatten the arc, equalise the endings, leave the stamp
up). Every visual is a device the game already ships, credited in the source:
the .pow slam, flyBall's sine arc, resolveShot's make/miss story, ev-confetti,
the ON FIRE stamp, audio.js's own synth recipes. A "today's version" toggle
plays the current whisper beside the theatre for contrast. The crowd cheer is
deliberately a labelled empty slot, per the rule below. **One collision caught
by its own screenshot pass: the first cut painted all five stops teal, which is
the corner-three colour-law break again; the tiles keep the difficulty scale
and the floor/tab/stamp carry "defense".**

**RULED by Aaron on the sample, 08-09:** *"I don't like the black background on
'on fire'."* **FIXED** in the sample: `onfire-stamp-a.webp` carries its black
plate in the pixels, and the game only hides it with `mix-blend-mode:screen`
over the near-black arena. The sample now keys the plate out for real at build
time (unmultiply-from-black: alpha = max(r,g,b), colour unpremultiplied), so
the flames float over any backdrop with no blend mode, and the check demands a
transparent corner pixel. **FILED for the real build:** ship the alpha-keyed
stamp as a committed asset (`onfire-stamp-a-alpha.webp` + a generator in
tools/) and retire the blend-mode trick in `#fireslam` too, so the game's own
slam stops depending on what happens to be behind it. The real build hooks `answer()`,
`roundBreak()` and `finish()` in daily.js, and writes the shot module ONCE for
both screens.

**Sequencing: the cheer can be filed and sourced in parallel with the visual
work.** The visuals should not wait on the audio, and the audio should not be
faked to unblock the visuals.

- [ ] **D4 · Right and wrong are a whisper.** Aaron: *"all you see is that
  little green or red wording over the question and it's such a short window to
  even see what it said and it's so quick... the player really needs to feel
  like they won or got it wrong."*
- [ ] **D5 · No shot on offence.** Aaron: *"it would be cool if you saw a ball
  go from that spot to the rim area and either swish or miss lol like that ball
  you had in the background of the coming soon page."*
  Medium check: an arcing ball, a trail, a net ripple and a rim clang are
  **vector, CSS and canvas geometry — build it, do not source it.** The ball art
  already exists (`assets/ball-hero.png`, `ball-roll.png`).
  **AND IT IS THE SAME JOB AS THE MAIN GAME'S.** V0's build list has "shot
  effects (arc trail, swish burst, rim rattle)" as not started. Build the
  effect once, drive it from both. Building a Daily-Five-only version is how
  the two screens end up looking like different games.
- [ ] **D6 · Defence does not announce itself.** Aaron: *"when we switch to
  defense, it's not even clear you are on defense, it's just 5 squares."*
  The mode already has two halves with different scoring and different stakes,
  and the screen barely says so. Wants a real transition, not a label.
- [ ] **D7 · The coach could carry some of this.** Aaron: *"maybe the coach can
  make these things clear too?"* The coach can now speak on a menu screen and
  carry a button (shipped 08-07), so a Daily-Five first-run word is cheap.
  **Careful though:** a coach explaining what a shot animation should have made
  obvious is a patch over D4-D6, not a substitute for them. Use him for the
  round change, not for the result of every card.

#### Round two of the playthrough, 2026-08-07 — four more, all found on a phone

- [x] ~~**D9 · THE MENU STOPPED WORKING AFTER DISMISSING THE COACH.**~~ **FIXED, and it was LIVE ON MAIN.**
  Aaron: *"you can't click vs CPU anymore when add to the Home Screen button
  option is on after you say not now to the coach... but then if you join back
  later after the coach has run once, everything is fine."*
  **Reproduced in one run, and his own report contained the diagnosis.** The
  fix: `#coachTip.onmenu{pointer-events:auto}` turned pointer events on for the
  class that says "this is a menu card" — and `tipHide()` only removed `.on`.
  So after dismissing, an **invisible, full-size card sat across the middle of
  the menu eating every tap.** `elementFromPoint` over the CPU button returned
  `.ct-anytime`, a span inside the hidden card. The second-visit clue is the
  proof: on a later visit the welcome never fires, `.onmenu` is never added, and
  nothing is swallowed.
  Two fixes, because one would have left the trap set: pointer events now
  require `.onmenu.on`, and `tipHide()` clears every mode class rather than just
  `.on`. **The check asserts on what is actually on top of each menu button**,
  not on whether a class is present, because the class is a proxy and the tap is
  the thing players hit.
  **This shipped. It went out in the install-prompt merge and was live for about
  an hour before he found it by playing.**

- [x] ~~**D10 · LEAVING THE DAILY FIVE MID-RUN STARTS IT OVER.**~~ **FIXED 2026-08-07, to Aaron's rule.**
  **He rejected both options I offered and wrote a better one.** Freeze-on-leave
  was his instinct and he talked himself out of it in the same sentence:
  *"I want to say freeze on leave but it's too gameable."* Correct — freezing
  turns backgrounding the app into free thinking time on a timed card.
  **THE RULE: leave mid-question and that question is simply wrong. You come
  back at the next one.** *"That question is wrong, just is what it is."*
  It is also the cheapest of the three to build, which is worth noticing: there
  is no remaining-clock to store, no timer to resume, and no disagreement
  between what the clock said when you left and what it says when you return.
  **The awkward state cannot exist, because leaving RESOLVES the card instead of
  suspending it.** A rule that is fairer and simpler at once is usually the right
  one.
  **AND YESTERDAY SCORES AS IT STOOD**, also his call. An unfinished run from an
  earlier day is banked into the history on next open rather than resumed, so
  the calendar shows what actually happened and no day's card set stays live
  after everyone else has moved on.
  Leaving is caught on four routes — navigating inside the app (`show()`),
  backgrounding the tab, `pagehide`, and `beforeunload` — because no single
  event fires everywhere and iOS is the one that punishes assuming otherwise.
  `tools/daily-resume-check.mjs`, **15 checks, three sabotages proven**,
  including one that fails if the abandoned card is NOT scored wrong, which is
  exactly the gameable version of this feature.

  **The bug the harness caught, and it is the same lesson as this morning's:**
  wiring one function to four events made a page RELOAD abandon THREE cards at
  once, because a reload fires `pagehide` and `beforeunload` and
  `visibilitychange`. Round 1 card 3 came back as round 2 card 1. **I had
  written "it is idempotent" in the comment above the function without checking
  whether it was.** It now guards per card, and a reload costs exactly one — not
  three, and not zero, because zero would make refresh the way to dodge a
  question you cannot answer.

- [ ] **D10 (original spec, kept for the reasoning) · leaving mid-run**
  Aaron: *"if you leave daily five in the middle of it, the player gets to start
  over, that's not good, they have to come back and with exactly the amount of
  time left on the clock for the question they were on."*
  He is right twice. It is a **fairness hole** — leave a card you cannot answer,
  come back, get it fresh — and the daily's whole premise is that everyone faced
  the same ten under the same conditions.
  **What is stored today:** the date, and the finished receipt. Nothing about a
  run in progress. `D` holds round, index, points, the two mark arrays and the
  seeded set, all in memory only.
  **What it needs:** persist `D` plus **the clock's remaining milliseconds** on
  every answer, on `visibilitychange`, and on `pagehide` — the last two matter
  because closing a tab fires neither `beforeunload` reliably on iOS nor
  anything at all if the phone just sleeps. On open, if there is an unfinished
  run for TODAY, restore it and start the clock at exactly the stored remainder.
  **Two decisions inside it Aaron should make:** does the clock keep running
  while you are away (harsh, and unfixable if the phone dies), or does it freeze
  on leave and resume on return (kind, and gameable by backgrounding to think)?
  And what happens to an unfinished run from YESTERDAY — lost, or scored as it
  stood? *Claude's read: freeze on leave, and yesterday's unfinished run scores
  as it stood, because a daily that punishes a dead battery is a daily people
  stop opening.*

- [x] ~~**D11 · A GLITCHY SCREEN BETWEEN OFFENCE AND DEFENCE.**~~ **FIXED 2026-08-07.**
  Aaron: *"there is a small glitchy screen in the transition from Offense to
  defense in the daily five."* Not a rendering glitch — **a layout snap, and it
  was the biggest one in the mode.** Measured across all ten cards at 390px:
  real question cards run **252 to 295px** and the ROUND 2 break card was
  **128px**, so the screen collapsed 145px and re-expanded two frames later,
  throwing everything below it twice.
  The break now fills the same box a question does, and every card swap fades
  over 160ms instead of hard-cutting. **Biggest jump between consecutive screens:
  167px before, 22px after.** Guarded by two checks that measure the heights
  rather than looking at them.

- [x] ~~**D12 · The pick still highlighted on the next question.**~~ **NOT A NEW BUG — it is D1, and D1 was not merged yet.**
  Aaron flagged it with *"maybe we just haven't merged yet idk"* and he was
  right. Confirmed: `origin/main` still carries the ungated `.dva:hover`. Worth
  keeping as a line because it is the second time a "still broken" report has
  turned out to be "fixed on a branch nobody merged", and the answer both times
  was to check `git` rather than to re-investigate the code.

#### Round three of the playthrough, 2026-08-07

- [x] ~~**D13 · The resume said nothing.**~~ **FIXED.** Aaron: *"there needs to
  be something that pops up to let a player know they closed out during the last
  card and so it's wrong."* The coach now says so on every resume, names the
  card as a miss, and says which card of ten you are picking up at. It fires
  every time rather than once per phone, because it reports what just happened
  rather than teaching how the game works — and silently resuming one card
  further along, carrying a mark you did not make, is indistinguishable from a
  bug.

- [x] ~~**D14 · The footer printed through the ONLINE button.**~~ **FIXED.**
  Aaron sent the photograph. **Headless at his width showed a 96px GAP**, which
  was the useful part: a pinned footer positions against the LAYOUT viewport and
  Safari's bottom toolbar eats about a hundred pixels of the VISIBLE one, so the
  menu keeps its height while the space shrinks. Reproduced by shortening the
  viewport instead — overlap at 360x740 and 390x667. It now flows after the menu
  instead of being pinned, so it cannot collide at any height with or without
  browser chrome. Measured clear at six sizes from 320 to 1280.

- [x] ~~**D15 · The round change, ACTUALLY fixed this time.**~~
  Aaron: *"there is still this weird screen... the court with the five questions
  but all the squares pushed to the left and then suddenly the defense screen
  shows."* His description was the diagnosis.
  Round 1 spots are absolutely positioned from their own court coordinates.
  **Round 2 spots have none, and depend on `.dvstage.r2` to make them static and
  hide the court — and that class was set only inside `showCard()`.** So
  `roundBreak()`, which flips the round and repaints the rack, left the stage
  saying `r1` for the whole 1600ms of the break: five stop-spots stacked at the
  top-left of a court that should not have been visible. The class now lives in
  `paintRack()`, with the thing it governs, so the two cannot disagree.
  **I CALLED D11 FIXED YESTERDAY AND IT WAS HALF FIXED.** The card-height snap
  was real and I measured it and corrected it — and never opened `paintRack`.
  **A symptom that survives a fix means the diagnosis was partial**, and the
  honest response to "it is still doing it" is to reopen the diagnosis rather
  than to re-explain the fix.

- [x] ~~**D23 · THE COACH'S SPOTLIGHT POINTED AT NOTHING ON THE NEW MENU.**~~
  **FIXED.** Aaron, 2026-08-08: *"big question, does the add Home Screen and
  coach still work with the new main menu?"*
  **Mostly yes, and one thing badly no.** The offer was live on the new menu's
  logo (`can-install` set, hint pill drawn), the coach's welcome fired, and
  tapping the logo opened the sheet. But `spotMove()` used
  `document.querySelector(sel)`, which returns the FIRST match in the DOM, and
  that is the CLASSIC menu's logo, which is `display:none` whenever the new menu
  is up. **The hole was being cut at `-12,-12` with a 24px diameter**: a
  spotlight parked off the corner of the screen, pointing at nothing, on the
  exact card that says *tap the logo*.
  Nothing threw. Nothing looked wrong in code. `install-check` was green the
  whole time because every one of its 54 checks ran against the CLASSIC menu,
  pinned there deliberately so its clicks would land.
  `spotTarget()` now walks the matches and takes the first with a non-zero
  rect, which is right for any future duplicate rather than only this one.
  **Seven new checks in `install-check.mjs` on the new menu**, and the
  geometric one was sabotage-tested: putting `querySelector` back prints
  `[-12,-12,24]` and fails, the fix prints `[153,34,85]` and passes.
  **The lesson is the question, not the bug.** He asked whether it still worked
  and the honest answer was that I did not know, because the harness that would
  have told me was pinned to the other menu. **A green suite proves what it
  tested, and it had stopped testing half the product the moment the second
  menu existed.**

#### THE PLACES · Aaron's big one, answered and parked (2026-08-08)

A walkable Gym, a walkable room, and a top-down town with a time machine
building. He asked *"how possible building that is"* and *"what do I need to
do."* **Full feasibility answer in `BUILD.md` section 6, THE PLACES.**

The short of it, so this line is useful on its own:
- **Yes.** It is a node graph of still images, the point-and-click adventure
  structure. The engine is a few hundred lines, about a Daily Five's worth.
- **The art is the project, and it is roughly twelve images**, not sixty,
  because the trick is ONE very wide image per room with the camera panning
  inside it rather than one image per viewpoint.
- **Aaron does not need to source anything new.** Firefly plus
  `design/COURT-SKINS.md` is the pipeline that already produced 27 court images.
  Claude writes prompts, Aaron generates and picks, Claude composites.
- **POST-LAUNCH.** Bigger than the rest of the roadmap combined and it must not
  sit in front of the twenty friends.
- **RULED 2026-08-09, and it changes the ART BILL, not the code.** Aaron, after
  running spike v2: *"I def need the near layer."* So every room is delivered in
  **LAYERS**, a background plus two or three transparent cutouts of whatever is
  nearest the camera, not one flat picture. That is what turns a push-in from a
  zoom into a step, and it is the one thing in this whole feature that cannot be
  added afterwards: ask for the cutouts at generation time and it costs one extra
  prompt per room, ask after twelve flat pictures are finished and it costs
  twelve pictures. **Nothing gets commissioned without it.** He also asked to
  feel a real pivot before ruling on turning, which is spike v3.
- **THE TIME MACHINE HAS ITS SOUNDS BEFORE IT HAS PIXELS.** Aaron's 08-09
  sound folder names three files for it: *Takes Off*, *Powers Up*, and *A Gear
  Turning Sound (picking eras)*. They live in `docs/play/assets/sfx/` as
  `time-machine-*.mp3`, measured in the manifest. Nothing here changes the
  post-launch ruling; it means the room arrives to a soundtrack.
- **ONE PIECE OF IT HAS SINCE BEEN SPLIT OUT AND IS IN V0: the Gym, as B14.**
  Not a walk-back of this ruling. The Gym is one room with no camera, no back
  stack, no town and no walking figure, which is the first sixty lines of the
  spike and none of the rest. The rest of THE PLACES stays exactly here.
- **The next move is a half-day SPIKE** using art already in the repo, to find
  out whether a push-in on a phone feels like walking or like a zoom. Nothing is
  commissioned until that answers.
- **THE ROOMS UPGRADE WITH THE CAREER (Aaron, 2026-08-09, filed for the
  backlog, his words condensed).** Two progressions, both post-launch, both
  bound by the layers rule above (nothing commissioned without cutouts):
  - **The Gym.** The starter high-school gym stays for now (*"even in small
    cities people go back to highschool gyms to train, it makes sense"*). The
    LATE-career gym is a **hallway you walk into**: to the LEFT, glass doors
    into your office; to the RIGHT, clear glass showing the weight room you
    can enter to work out; STRAIGHT AHEAD, half-open glass doors into a
    top-of-the-line training gym you can walk through. That is a four-room
    node cluster (hallway, office, weight room, main floor), not one image.
  - **The Bedroom. RE-RULED 08-16, his words:** *"I don't want a teenagers
    room, this is a college kid and then the room when they become the hall
    of famer at the end of career mode, also maybe there is an in between."*
    So THREE stages: the college room (start · the old teenager's-room
    prompt in the art brief contradicted the audience ruling and is
    superseded), the high-rise (his 08-09 *"dope highrise room"*, the
    in-between), the hall-of-famer's den (the end). The in-between's cheap
    half is now a SHOP mechanic: purchased decor composites into whatever
    room you own, so every room progresses between art stages for free.
    Prompts for all three: PLACES-ART-BRIEF TIER 3.
  - **THE CAREER ROUND JOTTED 08-16 (his list, from bed, not for the
    twenty).** Cutscene prompt family (walking to your room, walking to meet
    the coach, the time machine room and the era picker inside it, the shop
    front · all first person with nobody in frame, the player's look stays
    undecided) · GYM STAGE 2 ruled: the career gym CAN MATCH THE MAIN MENU
    arena, generate with arena-menu.jpg as the reference · THE SHOP is the
    fourth town building, career winnings buy COSMETICS ONLY (DESIGN § 11
    stands): court skins from the 27 already on disk, jersey palette swaps,
    room decor, era memorabilia unlocked by time machine trips, theatre
    accents. Fourteen prompts written, each one box under Firefly's 1024:
    PLACES-ART-BRIEF TIER 3 + the Places artifact. His 3:2 question answered
    there with the measured arithmetic: 3:2 walkable rooms, 16:9 cutscenes,
    9:16 phone backdrops, match the frame to the surface.
- **ART ROUND 2 (08-09, on Aaron's desk):**
  <https://claude.ai/code/artifact/c54731f8-60c0-4cdf-97f5-52a2433667dc>
  answers his widescreen-on-mobile question with his own Firefly gym samples
  (verdict: wide is RIGHT for rooms and tiles, wrong only for full-bleed phone
  backdrops, which generate 9:16 instead; the phone keeps 26% of a 16:9 image,
  measured; desktop answered 08-09 too: portrait full-bleed on a monitor keeps
  the middle ~35% band, so the plan is portrait first, pick the winner, then
  ONE 16:9 sibling of the winning take only, swapped by screen width; portrait
  alone survives desktop because the surface is veiled ambience) and carries
  five copy-ready prompts: Daily 5 backdrop x2 (PORTRAIT,
  recommended empty, no bystander crowd; distant-silhouettes line offered) and
  The Jacket tile x2 (jacket-as-subject recommended; the wide-room take doubles
  as the first high-rise bedroom concept), plus the Quick Run square (1:1, a
  ball frozen mid-bounce, the express lane, added later the same night when he
  asked). His gym batch lives in Drive (per-generator folders); full batch
  comes after he rules on the aspect verdict.

#### NEXT WEEK IS THE COACH (Aaron, 2026-08-08)

> *"I want to do all of the coach stuff next week."*

**Scheduled, not started.** Everything coach-shaped now moves behind this one
heading so it goes in as a block rather than in slivers:

| Item | What it is |
|---|---|
| **B7** | The coach as a first-run guide. Aaron moved this INTO Track B on 08-07. The biggest item on the track by a distance |
| **D7** | The coach carrying some of the Daily Five's theatre |
| The Gym as a ROOM | **Now IN V0. See B14 below.** Sample built 08-08, court corrected 08-09 |
| B5's drill defects | The coach card covering the board during drills · drills refusing off-drill moves |
| The `bk_install_seen` question | The welcome fires once per phone forever. Filed 08-07, unanswered |

**Why blocking it out is right rather than tidy:** these five all touch
`coach.js` and the same tip card, and they share one question nobody has
answered yet, which is **how much the Coach is allowed to say before he is
annoying.** Answered once, it settles all five. Answered five times, it will be
answered five different ways.

**THE TWO LISTS ARE WRITTEN: `design/COACH-AND-DRILLS.md`**, readable at
<https://claude.ai/code/artifact/26fb5cf8-5f2c-4d89-b860-65c8ba8ff7dd> (Aaron asked for
them 08-09: *"I need two lists and we need to go over them in great detail
because I don't want to miss A THING."*). **66 drill candidates** and **256
coach moments across 19 entry points**, every row with an id and a weight so it
can be cut by instruction rather than by argument.

**The finding that matters more than the lists:** weighting every coach moment
and then counting the weights gives **77 MUST moments on a first game**, which
is one card every fifteen seconds. The weight scale sorts the list and does not
cut it, because the game genuinely has that many moving parts. **So the proposal
is a BUDGET, not a trim: no more than twelve coach cards in a first game, never
two in one possession, and anything that does not fit WAITS rather than being
dropped.** That makes the list a priority queue instead of a script, and it is
the ruling that unblocks all five items in the table above. **It is Aaron's
call and it is the first thing to answer next week.**

**Two defects surfaced while counting, both with verdicts:**
- **FILED.** The Settings screen says the music is *"5 tracks"*. `audio.js` has
  **8** (menu, game, win, lose, tutorial, paused, daily, cursed) and the
  Rulebook says *"any of the eight"*. One of the two is wrong on the live site.
  Filed in `design/COACH-AND-DRILLS.md`; it rides B12's polish row.
- **RULED as a DESIGN gap, not a bug.** `foul` appears **zero** times in
  `game.js` and zero times in `index.html`, while DESIGN.md section 5 specifies
  fouls, free throws, the bonus and fouling out. The Rulebook describes contests
  without them, so the shipped game is internally coherent. Three drill
  candidates stay blocked until that gap is closed.

**TESTER SESSION #1 LANDED ON THIS BLOCK (Malik, 08-09 evening, filed 08-10,
index in BUILD.md 6c).** Four findings, all coach-shaped, none fixed yet:

- [x] ~~**D25 · THE COACH CARD THAT WOULD NOT LEAVE.**~~ **FIXED 2026-08-10,
  and the ":16" header was the confession.** The mechanism, measured: ending a
  drill runs `drillTeardown()` (drill flag off) then `show('how')`, and
  `show()` keeps the outgoing game screen `'on'` for its **440ms** slide-out
  while the coach watcher ticks every **700ms**, so ~63% of drill exits gave
  the watcher one tick where every first-run gate read true and the sticky
  card fired onto the Rulebook. Its header then trusted `BKDaily._hold(true)`,
  which vouched for a daily clock STILL ARMED minutes after the Daily Five
  (Malik's leaked :16, which would also have fired a phantom time-up later).
  Four fixes, four sabotages, `tools/coach-first-check.mjs`, 16 checks:
  the watcher holds its tongue while any screen is sliding out; `leaving()`
  clockStops unconditionally and `_hold(true)` off the daily screen kills the
  leak and reports 0; a session-memory `memSeen` makes a dead localStorage
  unable to re-fire the same tip forever; and a JANITOR tick tears down any
  modal card that outlives its screen and makes Coach off win within 1.4s
  whatever else is broken. Each fix reverted alone sends the harness red
  (proven 08-10). Bonus from the same dig: **8 escaped `—` em dashes**
  were rendering to players while evading the 0-gate, all replaced (middot
  for separators, colon for restatements, two sentences for two clauses) and
  `emdash.py` now counts the escaped form in copy.
- [ ] **D26 · THE DAILY FIVE HAS NO FIRST-TIME INTRO, and it costs a shot.**
  Malik tried to read everything on his first Daily Five and **missed the
  first shot while reading** (the timer does not wait for understanding).
  Aaron's ruling-shaped words: *"unlike games like sudoku... this isn't a
  known game so players need an intro to these things, looks like bringing
  the coach into the daily 5 is a must at least for the beginning."* This
  widens **D7** (coach carrying the Daily Five's theatre) from nice-to-have
  to must, and the cheap floor is: first-ever visit, the clock does not
  start until the first tap.
- [ ] **D27 · CADENCE, Aaron's own observation:** *"during regular gameplay
  the coach cadence is weird, gotta check that again."* No repro yet; it is
  exactly what the twelve-cards-a-game BUDGET proposal above exists to fix,
  and it is more evidence that the budget ruling is the first thing to
  answer next week.
- [x] ~~**D28 · PACK RARITY AND PLAYER TIER SHARE THREE COLOURS EXACTLY.**~~
  **MOSTLY SETTLED 2026-08-11 by Aaron's colour rulings** (his words, filed
  in BUILD.md § the palette rulings): Legendary is purple #a45cff in all
  three ladders, Epic is rose #ff4f7a, Hall of Fame keeps gold, and gold now
  means Superstar and ON FIRE only. Of the three exact overlaps: gold-on-gold
  GONE (the shot that named this finding), Epic-vs-All-Star GONE (rose vs
  purple, deltaE 30.0). **Still standing, two parts:** Common #9a8f7c is
  still exactly the Role badge grey (unruled, smallest of the three since
  both mean "ordinary"); and the NEW Legendary purple sits deltaE 12.0 from
  All-Star #b98cff on the same screen. Measured before shipping: no purple
  clears both All-Star by 20+ and a 4.7:1 contrast floor, so this is the
  best purple that exists, and the two never label the same THING (chip
  above, badge below). Judged acceptable from the real screenshot; his call
  to overrule.
- [x] ~~**D29 · THE BLUE TEAM AND A RARE PACK ARE BOTH #58a8d6.**~~
  **RULED 2026-08-11 · live with it.** Aaron closed the colour pass with
  *"Everything Else is fine"* after his four changes, so Rare stays
  #58a8d6. Reopen with a word if that is not what he meant. Original
  finding, kept because the measurement stands: Confirmed
  live, not inferred: with the blue side on the clock and the pack rolled to
  Rare, the header, the on-the-clock line and the rarity chip all render
  rgb(88,168,214) in one frame. Rare is weighted 28 of 100 and the blue side
  reveals every local two-player game, so it is roughly one game in four.
  Team colour should win the argument: move Rare, not the blue. Filed
  2026-08-10 (F4).
- [x] ~~**D30 · TWO LEAGUE ACCENT PAIRS ARE TOO CLOSE TO TELL APART.**~~
  **RULED 2026-08-11 · live with it** (*"Everything Else is fine"*, same
  ruling as D29). Original finding: Flags
  #6fd0c3 and Overseas #4e9c93 at deltaE 18.9, sitting as adjacent rows on
  the picker, and BIG3 #d8b25a with Street Legends #c08a5a at 23.2. League
  accents are pure identity with no scale to respect, so any distinct hue
  fixes it; this is the cheapest item on the list. Filed 2026-08-10 (F3).
- [x] ~~**D31 · HARDWOOD AND THE GARDEN ARE THE SAME THEME IN PLAY.**~~
  **RULED 2026-08-11 · live with it** (*"Everything Else is fine"*, same
  ruling as D29). Original finding: Both set
  `--accent:#f5872e`; they differ only on `--accent-deep`, which paints rims
  and shadows while accent carries every player, button and highlight. A
  player picking The Garden for Knicks blue gets a court that looks like
  Hardwood. Either promote #1f4f9c to the accent or retire the theme. Filed
  2026-08-10 (F5).
- [x] ~~**D32 · DESIGN.md AND THE GAME DISAGREE ABOUT THE TURN.**~~
  ✅ **RULED AND BUILT 2026-08-11.** Extended same day on his read: the turn
  banner now says **Free step available** until it is used, and his asked-for
  nudge shipped · the first main action ever attempted without the step
  raises the coach once ("Want your free step first?"), the action is kept
  staged, and the second tap plays through, guarded against trapping by
  three checks in turn-economy-check (now 10). The artifact's after-frame
  was WRONG on first publish (the tap grabbed the ball-handler, Aaron
  caught it); retaken, and the capture script now refuses any frame whose
  own DOM lacks the FREE label. Before/after:
  <https://claude.ai/code/artifact/1305e07b-1615-4036-8374-faa58b2f6763>
  Aaron: *"Design free off ball movement
  please and make sure it's clear in the in game coaching and drills and
  rulebook."* Shipped exactly as DESIGN.md 3 promised: ONE free off-ball
  step of exactly 1 square per offensive turn, plus the main action. Longer
  repositioning still costs the action. One predicate
  (`freeStepQualifies()`) answers for the stage label, the commit hinge and
  the harness, so the button can never promise what the move will not do.
  Clarity, all four asked-for surfaces: the stage label says **FREE step**
  and the button says **Free step ✓** BEFORE you commit (the visible-
  incentive constraint from 08-02) · a banner after · a once-per-phone
  coach tip on first use · a new **free step drill** (`space`, 11 drills
  now) · the rulebook's Your Possession topic rewritten with its own RUN
  button. `tools/turn-economy-check.mjs` flipped from the deliberate
  failure to the guard: 7 checks, all five economy claims plus a sabotage.
  The CPU plays the same rule (its off-ball fallback earns the same free
  step; `shuffleUsed` makes a loop impossible), and the flag rides the net
  snapshot so a reconnect cannot mint a second step.
  ~~Old finding:~~ § 3 line 68 promised the shuffle and the game gave none; § 3 line 68 states *"Per offensive turn: one free
  off-ball shuffle (1 square) + one main action"*. The game gives no free
  shuffle: `tools/turn-economy-check.mjs` moves an off-ball attacker one square
  and the phase goes straight to `def-slide`, so the shuffle spent the
  possession. **Fix one of them, never leave both** — a doc stating a rule the
  game does not implement is worse than either, because everyone who reads it
  reasons from it. The harness fails today and goes green when they agree.
  Aaron, 2026-08-10: *"I am pretty sure that we moved to all movement before
  the main action is free (1 per position) and so I am not sure why that never
  shipped to the game."* Two separate decisions sit under that memory and
  neither was built: this one, and the team-turns toggle at BUILD § FL-2.6 /
  open question 10, which was only ever dispositioned as "prototype behind a
  toggle and playtest". **Recommendation: build the free off-ball move, strike
  nothing.** It is the smaller change, it is what the doc already promises, and
  22af F4 rates it the highest-value single change in the run: one rule fixing
  idle pieces and possession length together, no timer.
- [ ] **D37 · FULL SETUP IS RULED; THE DEFENSIVE RESPONSE GOES TO RESEARCH.
  Aaron, 2026-08-11: "It should be all pieces move free, and we need research
  as to what the correct defensive response is, because one player moving 2
  squares as response to the whole team feels unfair."** Three consequences:
  the model is decided (every off-ball player may step 1 square free before
  the main action · no more one-step-only); my 2-square-slide proposal is
  REJECTED in his words; and the defensive rule ships nothing until the
  comparative research run returns and he rules on it. Research brief
  delivered 08-11: `design/d37-defense-brief.md`, locked list at BUILD 22ah.
  **TWO RUNS RETURNED, recommendation REVISED between them (08-11 hand run,
  08-12 nine-agent workflow whose verify pass refuted the hand run's
  cleanest claim and demoted its Rotation proposal). On his desk now: THE
  CALL AND THE SLIDE · one visible coverage call BEFORE the offense's free
  setup (one tap from a 4-5 call menu, painted on the court, honest and
  coarse) + the existing one slide after the main action (Kill Team's
  Counteract shape, verified) + turnover risk as the equalizer. Runner-up:
  the Rotation. No mid-beat prompts ever. Findings v2 at
  `design/d37-defense-findings.md`, full story at BUILD 22ah. HIS RULING
  DECIDES; paper-test the call menu first.**
  **AARON'S OWN THEORY, 08-12, filed in his words because it converges with
  the research from the sport side:** *"Standard inbound - defense chooses
  from a list of defensive setups on their side of the court, offense sets
  up a cutter and so does defense and the play starts, offense has full team
  free movement and the defense has 1 person slide (full motion) and then
  offense has an action. This continues until the opposing inbound."* And on
  live-ball turnovers, self-answered in the same message: steals and
  defensive rebounds get NO reset, play continues, *"because that's the
  advantage that the team who stole the ball or got the rebound gets, the
  other team cannot setup. Yup."* Mapping to the recommendation: his
  setup-list IS the coverage call expressed as positions (honest and
  on-the-board by construction); his beat order re-times the existing slide
  to fall AFTER the offense's free setup and BEFORE the main action (same
  one-input-per-beat economy, fresher information); the cutter designation
  is new design space (pairs with 22ac 26's spotlight-matchup idea). Jump
  ball groupings: his own second thought (reuse the standard possession
  setup after the toss) is the cheaper build. NOT RULED YET; the paper
  playtest should test HIS version (setups + re-timed slide) against the
  research version (abstract calls + post-action slide).**
  **DRAWN 08-12 at his ask ("I want to see what this would look like
  visually"): the full possession walkthrough, tip to opposing inbound to
  the no-reset fast break, eleven frames on the game's own court
  proportions, each with a VS TODAY note:
  <https://claude.ai/code/artifact/a3d8a94c-9bff-4614-8979-a468e62f1f9f>**
  **HIS QUESTION SET off the walkthrough (08-13), with two attribution
  corrections owed to it:** (1) the "1 square per player" size of the free
  setup was INHERITED from the shipped one-step rule, never separately
  ruled; his ruling was "all pieces move free" and the number is OPEN.
  (2) The CUTTER and the four setup names in the walkthrough (protect
  paint / chase arc / shadow star / press) are ILLUSTRATIVE, invented to
  show the device; no list is chosen, no cutter ability designed, and the
  gold ring in the frames is diagram tracking, not a game state. Also
  corrected in the artifact: an open short pass is FREE in the shipped
  game (measured in doPass()); cards fire on risk only. **His new leaning,
  his words: offense picks from a list of setups too, "I think this might
  be the one"** · it also answers his own how-do-they-get-up-the-court
  question (today pieces stay where the play died and walk up; a setup
  list places the formation at the inbound). Cutter mechanics if he wants
  it to BE something, options filed: marker only · cutter's free step is
  2 squares · mid-cut passes get a tier break · defense's shadow cancels
  it when adjacent. Play-calling research queued at BUILD 22ai, to run
  only if he rules the calls in.**
  **HIS NEXT-STEPS DIRECTIVE, 08-13, five items in his order:** (1) the
  playbook research is GO, both sides, NBA/WNBA focus with the era/league
  lens, each play returned as a FULL PACKAGE (inbounder location and
  position, the receiver, that defender, every other player) and setups
  are starting shapes only ("All movement after setup is the players") ·
  brief written, `design/22ai-brief.md`, two runs; (2) he chooses the two
  lists from the return; (3) the free-movement NUMBER is the named open
  question: one tile per off-ball player, or every off-ball player moves
  its full range before the ball moves; (4) the defensive single mover's
  range is the other: 1-2 tiles, or full movement by role, still one
  chosen defender; (5) BUILD IT PLAYABLE (Track B row B16): Method B
  behind the staging flag with BOTH open numbers as toggles so his friend
  playtest settles them by feel, coach and drills silenced in prototype
  mode, and a hard revert: save point on main plus flag-off as the
  instant undo. The walkthrough artifact gets REDONE LATER, his call
  same message ("we need a new artifact anyway, but that's for later"):
  the free-pass frame still reads as carded because the card graphic sits
  in the frame regardless of the corrected caption.**
  **STEP 2 IS ANSWERED, 08-16, his words: "nahh I liked your recs." The
  default lists stand: defense MAN · 2-3 ZONE · BOX-AND-ONE, offense
  HORNS · FIVE-OUT · FLOPPY as the universal three. His three follow-up
  questions (what a pick MEANS on a made-basket inbound with the full
  court to go, what the defense is doing meanwhile, and why offer Zipper
  at all if it is the only sideline shape) are answered move by move in
  UP THE FLOOR, drawn on the full 15x8 board:
  <https://claude.ai/code/artifact/0c7f4d68-a36c-4575-9ebb-ffad9e628a9a>.
  Three PROPOSALS ride in it, none ruled: (1) THE TWO-PART ADVANCE SHAPE ·
  picking a setup after a made basket pre-stations three players at their
  frontcourt spots while the inbounder and receiver bring the ball up over
  two or three beats of the normal open-movement rules, no teleport and no
  five-man march; (2) the defense's made-basket choice is SET UP BACK in
  one of its three setups OR spend the pick on the DIAMOND PRESS, a
  contextual fourth option at made-basket inbounds only: five defenders
  committed up the floor and the inbound itself contested; (3) THE
  ALWAYS-THREE-UNIVERSAL MENU RULE, the fix for the single-option flaw he
  caught ("if zipper is all there is when sidelining inbounding, why is it
  even a choice"): the universal three appear on EVERY menu and the spot
  specialists JOIN them (baseline adds Box and 4-Low for five options,
  sideline adds Zipper for four), so the real choice is
  specialist-wins-the-catch versus generalist-wins-the-shape-after. Paper
  playtest, then his ruling.** **Build order until he rules:**
  the offense half gets built behind the staging flag (live players keep
  one-step so the live game never plays an economy he has already called
  unfair; he and his brother flip the flag in Settings to playtest all-free
  today). The defence-side nudge ("end a turn without moving all their
  players") builds WITH the final defensive rule.
  ~~The reopened question, kept for the record, originally filed as:~~
  **ONE FREE STEP OR FULL SETUP: Aaron reopened the model, 2026-08-11.** His words, after the one-step version shipped: *"I thought the
  research revealed that we should make all movement (one player) free before
  the main action. Is this incorrect? I see you saying one move, and that's
  it."* The honest reading of the sources: **22af F4 names the SHAPE (safe
  setup before the gamble) and never picks a NUMBER.** Blood Bowl itself moves
  EVERY player every turn; DESIGN.md's July line locked the narrow one-step
  version, pre-research; his memory is the fuller Blood Bowl shape. Both are
  legitimate readings.
  **The recommendation, using his own 08-02 spacing precedent: a playtest
  toggle.** "Free movement: One step (default, shipped) / Full setup (every
  off-ball player steps 1 free before the action)". He and his brother
  playtest; the twenty never see the toggle. Full setup's defensive rule if
  it wins: the defence keeps ONE answer per action but its slide becomes 2
  squares (it watched the whole setup); mirroring setup-for-setup is team
  turns (BUILD FL-2.6), which A7 explicitly says not to build on research
  grounds. Pace is the real risk: full setup adds up to 4 tap-and-confirm
  cycles to EVERY possession on a phone.
  **Also filed under this:** his defence-side nudge ("same for Defense wanting
  to end a turn without moving all their players") only exists under Full
  setup, since today's defence moves one man by design. Build it with the
  toggle if Full setup wins.
- [x] ~~**D33 · IF THE FREE MOVE SHIPS, THE DEFENCE MUST NOT ANSWER IT.**~~
  ✅ **SHIPPED WITH D32, 2026-08-11, exactly as proposed:** the free step
  draws NO defensive slide; the defence answers the main action, so the
  one-for-one exchange measured fair on 08-10 holds on everything that can
  score. Locked into DESIGN.md 3 and asserted by turn-economy-check
  claim 5 (step drew no slide, the action drew exactly one).
  ~~Old text:~~ Aaron,
  same message: *"we need to see how to have defense respond correctly."* Today
  every offensive action triggers a defensive slide, which is why the economy
  measured fair. Make off-ball moves free and that one-for-one breaks: either
  the defence answers each shuffle and no possession ever ends, or it answers
  none and the free moves are unopposed. **Proposed rule, needs Aaron:** free
  off-ball moves draw NO response, and the defence gets its slide when the
  main action commits. That preserves the current exchange on everything that
  can score and stops the defence having to answer a man stepping sideways.
  22af A7 argues the same shape from the other end: if the waiting player
  disengages, give the defender something to do inside the beat that matters,
  do not rebuild the turn system. Depends on D32.
- [ ] **D34 · THE OPEN-MAN BONUS, the half of the free move still open.**
  The free step shipped (D32) with real intrinsic uses: spacing, screens,
  cutting to a passing lane, rebound position, and the drill teaches
  exactly those. What did NOT ship is F5's open-man BONUS (end the turn
  uncovered, get something for it), because WHAT the bonus is has taste in
  it and Aaron's 08-02 ruling binds any version to a VISIBLE on-screen
  incentive. Options to put in front of him next batch: a heat drip for
  ending open · a one-tier discount on the next catch-and-shoot · nothing,
  if playtests show the step earns its keep unassisted.
  ~~Old text:~~ A free
  move nobody wants is extra tapping. F5's answer is an open-man bonus: end the
  turn uncovered and get something for it. Aaron ruled it 08-02 with a binding
  constraint — *"I LOVE THIS"* plus every incentive must be VISIBLE on screen.
  Zero matches for `openMan` / `unguarded` in `game.js`, so neither half was
  built. Do it with D32 or the free move lands inert.
- [ ] **D35 · THE PACING ANSWER NOBODY CLAIMED (22af A4).** Three studios with
  primary sources all pulled the same two levers when their games ran long:
  speed the middle, escalate the end. **No verified case anywhere of lowering
  the win target**, so 11 stays. Neither lever exists in the game. This is the
  standing answer to the pacing complaint and it has been sitting unbuilt since
  08-02.
- [ ] **D36 · THE TOURS DOC OWNS RULINGS NO HARVESTER READS.** D32 was in fact
  found and written down on 08-10, as item 4 of "Open for Aaron to rule" in
  `design/COACH-TOURS-2026-08-10.md`. It never surfaced, because
  `tools/open-items.py` harvests the five root docs only, and `next.py` reads
  V0's two tables. **A working doc under `design/` can accumulate pending
  rulings that are invisible to both commands that exist to answer "what is
  owed".** Fixed one way already: `open-items.py` now warns when a `design/`
  doc contains an open-rulings section. The rule stays that items live in their
  root home; the warning catches the ones that do not make the trip.
- **DECISION FOR AARON, not a defect: should a wrong answer be Hard red?**
  #d5524b is Hard difficulty, Pro level and a wrong verdict. The card you
  just failed was very often not a hard card, so turning it hard-red at the
  moment you miss may teach the wrong lesson; the correct-answer green
  #2fbf6a has the mirror problem at 16.1 from Easy green, closer than any
  pair inside the difficulty ladder. Taste, so it is his. Filed 2026-08-10
  (F8). Two more logged and deliberately NOT filed as work: the two court
  reds (Hard tiles vs the contest ring, CIE2000 3.4, saved by silhouette)
  and pack Legendary vs Hall of Fame (deltaE 5.1, already covered by D28).
- **THE FINDING THAT IS BIGGER THAN A DEFECT: Malik never saw the game, only
  the quiz.** He passed to a big man to beat a press, tried to cross up
  Steph Curry with him, and when Aaron asked why: *"I wasn't even paying
  attention to that, I was just answering the questions."* A first-time
  player treats the board as decoration and the trivia as the whole game,
  so the strategy layer (matchups, who you cross, who takes the shot) never
  reached him. **This is B7's real job description**: the first-run guide
  has to teach that the BOARD decides what your knowledge is worth, or the
  north star ("knowledge is your athleticism, strategy is your coaching")
  is only half-delivered. Filed as B7 scope, not a new item.

#### B14 · THE GYM AS A ROOM, pulled INTO V0 by Aaron, 2026-08-09

> *"I do want to try and get the Gym room ready for the 20 if it's possible!
> It'll be the last thing we do before release."*

**IN SCOPE, and it is the LAST item before release.** Everything else on Track B
goes first; this ships on top of a finished build or it does not ship. That
ordering is Aaron's and it is also the thing that makes saying yes safe: if the
art does not land in time, cutting B8 costs nothing, because nothing is built on
top of it.

**Why it can be in V0 when THE PLACES cannot.** They are not the same job and it
took building the sample to see it. THE PLACES is a node GRAPH: many rooms, a
camera that pushes in and pulls back, a back stack, a town, a walking figure,
about twelve commissioned images. B8 is **one room, one image, seven hotspots,
no camera, no walking, no graph.** The seven destinations already exist and
already work. Measured against the spike, B8 uses roughly the first sixty lines
of it and none of the rest.

**What actually has to be built** (the honest list, not the hopeful one):
- A `gym` screen: background image, scrim, seven positioned buttons, a back
  control. Everything else already routes.
- Each button calls the drill that `coach.js` runs today. No new drill content.
- Point The Gym menu row at the room instead of at the Rulebook, and decide
  where the Rulebook then lives (see the open question below).
- Small screen behaviour: the seven labels only just fit at 430 px and they were
  measured to get there. At 390 px they need re-measuring, which is
  `python3 tools/gym-labels.py` and not a rethink.
- `gym-check.mjs`: seven buttons present, each reaches its drill, back returns,
  nothing overlaps at 390 and at 430.

**THE ART SPEC CHANGED ON 08-09 and B14 is the first thing it touches.** Spike
v2 measured that a near layer moving faster than the far one is 8 to 9 percent
of the frame at rest and **over 80 percent mid-move**, which is the difference
between a zoom and a step. That means a room is not one picture, it is a
background plus two or three TRANSPARENT CUTOUTS of whatever is nearest.
**Ask for the layers in the same generation pass as the background.** One extra
prompt now; twelve regenerated pictures later. Full finding in `BUILD.md`
section 6, spike v2.

For B14 specifically this is a nice-to-have, not a blocker: the Gym as scoped
below has no camera, so a flat image still ships. But the gym interior is the
FIRST room that gets generated, and if it comes back flat it is the first room
that has to be redone the day THE PLACES starts.

**THE PROMPTS ARE WRITTEN: `design/PLACES-ART-BRIEF.md`** (Aaron, 08-09:
*"lets get me some prompts so I can go get some art and come back"*). Six
generations, three Gym facings and their near layers, and everything else in THE
PLACES is deliberately held until the Gym proves the pipeline. **Only facing 1
is needed for B14.** The other two exist because the room Aaron described has
the hoop ahead, weights to the right and the desk to the left, which is the
walk-and-turn feature exactly, and facings generated weeks apart never match.

**The one real dependency, and it is on Aaron's desk:** a gym interior image.
The sample uses a packed arena as a stand-in and a gym is the opposite of a
packed arena. Spec, so it can be generated in the same Firefly pass as
everything in `design/COURT-SKINS.md`: an EMPTY practice gym, wide, eye level or
slightly above, wood floor, cinderblock or banked bleachers, warm practice
lighting, nobody in it, 2400 px or wider, and shot WIDE because the phone frame
crops hard. The spike measured that crop: **a 16:9 image in a 390 px portrait
frame shows 36% of its width.** Anything that matters must live in the middle
third.

**BOTH OPEN QUESTIONS RULED by Aaron, 2026-08-09:**

> *"the rulebook should def be its own station at the gym, finished drills can
> show it"*

- **The Rulebook is the EIGHTH STATION.** A clipboard on the sideline, in the
  room, not a separate door off the menu. Consequences, so this is a decision
  and not a sentence: the Gym becomes the only way into the Rulebook, so the
  room has to load fast and has to be reachable from inside a game (the Rulebook
  is openable over the board today and that must survive). CM-GYM-08 in
  `design/COACH-AND-DRILLS.md` is answered by this, and the Rulebook's own
  coach moments now live under the Gym rather than under their own heading.
- **A finished drill shows that it is finished.** About six lines and one
  `localStorage` key. Needs a shape decision at build time, not now: a tick on
  the puck, the puck going from hollow to filled, or a count in the header
  ("4 of 7"). All seven done should be its own small moment, which is CM-GYM-05.

Nothing else about B14 is open.

**Where the work already is:** `docs/dev/gym-sample.html` and
`docs/dev/gym-sample.png`. The half court in it is now
`docs/play/assets/halfcourt.svg`, drawn to the real dimensions, every
measurement written into the file.

#### B4 · shipped 2026-08-08, and it was HALF the job I said it was

- [x] ~~**The wake lock.**~~ **DONE.** A phone sleeps after about thirty seconds
  of no touches, and this game has long stretches with nothing to touch: reading
  a card, watching the CPU take its turn, waiting for the other phone. The screen
  going dark mid-possession pauses NOTHING. The shot clock runs, the room waits,
  and the player has to wake the phone and work out what they missed. V0 files
  it under *"the game appeared broken"*, which is the right category: nothing is
  broken, it just behaves like it is.
  Held only where the player watches without touching, because a wake lock costs
  battery: **game, versus, brains and the Daily Five.** Never a menu.
  **The part that is easy to get wrong, and is the reason this needed a test:**
  the browser RELEASES the lock itself whenever the page is hidden and does not
  hand it back. So the re-acquire on `visibilitychange` is not belt and braces,
  it is the difference between the lock working ONCE and the lock working. Switch
  apps to check a score, come back, and without it the screen sleeps again.
  Feature-detected, every failure path a silent no-op. 12 checks in
  `tools/wake-check.mjs`, including a phone with no `wakeLock` API at all.

- [x] ~~**Handle the sleeping server gracefully.**~~ **ALREADY DONE, and I said
  otherwise.** I told Aaron B4 was two pieces of work. Measured before building:
  `netPoke()` pre-warms the relay the moment Online is tapped, `netDial()` runs
  an 85 second budget with **six escalating messages and a live seconds
  counter**, and a 10 second hang guard re-dials a socket that opened neither
  way. That is graceful. Nothing was needed.
  **The lesson is the ordinary one and it keeps costing:** I described the state
  of the code from the roadmap rather than from the code. The roadmap said the
  item was open, and the item was half closed by work done under a different
  name.

#### B3 · THE INVITE LINK — shipped 2026-08-08 (half of B3)

- [x] ~~**An invite link that just works.**~~ **DONE.**
  **The arithmetic V0 was pointing at:** a friend typed a 4 letter ACCESS code,
  then you texted them a 4 letter ROOM code, then they typed that. Two codes,
  four typing events, every one a place to give up. Now: you tap **Send the
  invite**, they tap the link, they are in the room.
  **Aaron ruled the gate STAYS ON and the link carries the pass** (08-08,
  picked over retiring the access code outright). So the people he invites never
  meet the bouncer, and somebody guessing a four letter room code still cannot
  get in. He keeps the door without charging his friends to walk through it.
  `?join=ABCD&k=PASS`. The pass is STORED rather than used once, because
  reconnects, a refresh mid-game and the rejoin path all read `passGet()`, and a
  link that got you in but not back in would be a worse bug than the one it
  fixes.
  The room code stays printed above the button for anyone who would rather read
  four letters down the phone. On a phone the button opens the real share sheet,
  which is how this link is actually going to be sent.
  **17 checks in `tools/invite-check.mjs`, six of them sabotage**: a 3 letter
  code, a 5 letter code, punctuation, an empty code, a key with no room, and a
  key with no room must not be STORED either.
  **Named honestly: the pass rides in a URL**, so anyone it is forwarded to
  inherits it. For a twenty person alpha that is the intended behaviour and not
  a leak. The kill switch is rotating `BK_ACCESS` in Render, which dies every
  old link at once.

- [ ] **THE OTHER HALF OF B3 IS AARON'S**, and it is one field.
  The relay turns its own gate off when `BK_ACCESS` is empty
  (`CODES.length > 0` is the only switch), so nothing here needs a deploy. **He
  does not have to do anything**: the link now carries whatever code is set, so
  the alpha works gated. This is only the lever if he ever wants it fully open.

#### Round four, 2026-08-08 — two asks and a testing door

- [x] ~~**D16 · THE COACH TALKED OVER A RUNNING CLOCK.**~~ **FIXED.**
  Aaron: *"Make sure the coach popup pauses daily 5 gameplay."*
  The Daily Five does not run on the engine, so `BK.freeze()` — the thing every
  coach tip already called — was a **no-op on that screen**. The resume notice
  D13 added fires one line after `showCard()` deals the card, so the very first
  thing a returning player saw was an explanation of why they lost a card,
  printed over a question whose clock was already burning. Reading it cost you
  the next one.
  `clockHold()` parks the remaining milliseconds and hands them straight back:
  a 17s card interrupted at :11 resumes at :11, and `clockTotal` never moves so
  the bar means the same thing before and after. `coach.js` calls it last, after
  the card is definitely up, and uses the returned milliseconds to decide
  whether the card may claim to have stopped anything — a menu tip on the title
  screen holds nothing and must not say it did.
  **It also puts the veil up whenever it holds.** A non-modal tip is
  click-through by design; on the daily screen that would have meant four live
  answer buttons under a card announcing that the clock was stopped — stopped
  for the timer, not for your thumb. Measured: `elementFromPoint` on an answer
  returns `coachVeil`.
  **NOT the pause loophole `clockStart` refuses to open.** That one is about a
  door the PLAYER can open — the streak calendar, which is still disabled
  mid-card. Nobody can summon a coach card.
  **And the cue moved because the screenshots said so.** The bar grows a striped
  HELD state, and the before/after showed the coach card sitting squarely on top
  of it at 390px *and* at 1440. So the frozen time is printed in the card's own
  header — `COACH · CLOCK STOPPED AT :21` — which doubles as a promise of
  exactly what you get back. The striped bar stays for wherever it is visible.
  A cue nobody can see is not a cue.

- [x] ~~**D17 · No way to replay the Daily Five for testing.**~~ **FIXED.**
  Aaron: *"I want to test again after you fix, can you reset the daily five
  somehow?"*
  Two URLs, deliberately not a button:
  **`?daily=reset`** clears today only — the stamp, the receipt, a half-finished
  run and today's row in the history — and lands you on a live card. Same ten
  questions, because the set is a function of the date. Your streak survives.
  **`?daily=wipe`** clears all of it, plus `bk_coach_seen`, so every coach tip
  fires again like a new phone. It is also how you get a FRESH ten: with the
  history gone, every past day is playable again from the calendar.
  A visible "replay today" control would be a re-roll, and a streak you can
  repair from a settings switch is not a streak. Anything not exactly `reset` or
  `wipe` does nothing at all and leaves you on the title screen — proved in the
  harness, because a door this destructive gets exactly two keys.
  It ships with the game's first toast (`#bkToast`), which says which of the two
  it did.

- [x] ~~**D18 · Online was fifth on the menu.**~~ **FIXED (a Track B ranking, not
  a bug).** Aaron: *"can we move online play to right below CPU? It's the second
  best Main attraction honestly."*
  Now **01 Vs the CPU · 02 Online · 03 Local VS · 04 How to Play · 05 Packs**.
  Nothing in the menu is positional in code — ids carry the handlers, and the
  only `nth-child` rules are the staircase indent and the entrance stagger,
  both of which follow position on purpose. Only the printed 01–05 had to move
  with it. The harness checks that the printed numbers and the on-screen
  geometry agree, because a list that reads 01,02,03 while the DOM says
  otherwise is worse than either error alone.
  Ranking matters more than it looks: the numbered list is read as *what is this
  game for* long before it is read as a list of buttons, and Local VS needs two
  people in one room where Online needs two people anywhere.

- [x] ~~**D19 · THE CLASSIC MENU CAUGHT UP: numbers gone, two renames.**~~
  **FIXED.** Aaron, 2026-08-08: *"on the old main menu you have to make some
  changes, get rid of the numbers next to each menu item, rename the how to play
  and the packs and more to the gym and the career mode."*
  `How to Play` is now **THE GYM** with *seven live drills · how to play*
  underneath, and `Packs & My Squad` is the career mode. Removing the five
  numbers also freed the `padding-left:50px` that existed only to clear them,
  which was 32px of dead space on every button.
  **Quick Run on the classic menu is deliberately NOT done** and he said so:
  *"we can figure out quick run on the old menu later."*
  One thing the screenshot caught: the COMING SOON ribbon was centred, straight
  across the button label. Survivable while that label was "Packs & My Squad",
  not once it is the career mode: the shot read `THE CO[COMING SOON]`. It sits
  in the top-right corner now, where the new menu already puts it, so both
  screens say COMING SOON the same way.

- [x] ~~**D20 · The career mode wore the same wallpaper as the game.**~~
  **FIXED.** Aaron: *"let's change the background of the career mode so it's not
  the same one as the background of the game."*
  The first build put `arena-menu.jpg` behind the hero, which is the image
  already sitting behind the entire screen. His instinct is right and the reason
  is bigger than variety: **a career mode is somewhere you GO, and a door onto
  the room you are already standing in is not a door.**
  It is now `courts/blacktop-a-bgwide.jpg`, a playground at sunset with a city
  skyline behind it. **Still no new art invented** and nothing to source: that
  file is already in the repo, already licensed, already shipping as a court
  skin. It is also the mode's premise in one picture.
  **Treated differently on purpose, which is the part worth writing down.**
  `#worldbg` dims hard and lays a `mix-blend-mode:color` tint over everything so
  the arena never competes with the type. This one keeps its own colour and
  takes only a scrim, because here the picture IS the pitch and a tint would
  flatten a sunset into orange paint. A deliberate divergence from the shared
  device, noted in the CSS so the next person does not "fix" it back.

- [x] ~~**D22 · The career row still wore a fan of card packs.**~~ **FIXED.**
  Aaron, 2026-08-08: *"have to change that pack of cards icon from the come
  up."* Right, and not only because it was leftover furniture from
  "Packs & My Squad": **a fan of packs promises LOOT, and this mode is about a
  rise.** The icon was quietly making a different offer than the label.
  It is now **three banners hanging from a rafter beam**, one lit and two dim,
  which is the progression fantasy in three shapes: you have one, there is room
  for more. Pure geometry, three rectangles with a V notched out by
  `clip-path`, so no illustration and nothing to source. The hover keeps the old
  fan's stagger rather than inventing a new motion.
  **Three placements to get the row right, and only 3x rendering found them.**
  Centred, the SOON ribbon lay across the label (`THE CO[COMING SOON]`).
  Top-left, it lay across the label too, because a 28px ribbon plus a 15px label
  does not fit in the 27px above it. Top-right, it lay across the rafter BEAM,
  so the banners appeared to dangle off the ribbon. The fix was to move the ICON
  rather than the ribbon and do the arithmetic: 64px row, ribbon 6 to 30,
  banners 32 to 58. Two things stacked, not two things overlapping.
  **COMING SOON became SOON, on BOTH menus.** The longer word measured 107px
  after the skew and clipped the label by 15. Changing it in one place only
  would have left the two screens saying one state two ways.
  The lock line was also being cut mid-word (`YOUR RUN · YOUR ROOM · YO...`),
  measured at 24 characters of room, so it is now `Your run · your room`. The
  fuller line still lives on the new menu's hero, which has the width for it.

- [x] ~~**D21 · THE CAREER MODE HAS NO NAME.**~~ **RULED 2026-08-08: THE JACKET.**
  Aaron: *"let's go with The Jacket and from there we can figure out the rest,"*
  with *"I'll workshop the name if we have to change it."*
  The Hall of Fame jacket. It is the only candidate that is a real OBJECT, so it
  can be drawn, put in the room, and earned, and career modes are better when
  the prize is a thing rather than a status.
  It is `CAREER_NAME` in `game.js` and every surface reads it from
  `[data-career-name]`, so workshopping it later is one line. Written that way
  because the name appears on both menus, and the same thing called two things
  on two screens is a bug this project already shipped once with the Daily Five
  tier names.

#### And one repeat, which is the interesting one

- [ ] **D8 · THE STAMP STILL DOES NOT READ AS TAPPABLE.** Aaron: *"the daily 5
  icon still doesn't blink enough or hover up and down enough to show it's
  clickable."*
  **This was already fixed once, on 08-05**, when the pulse was measured as
  lighting the ring only 12% of the time and was rebuilt as a resting ring plus
  a continuous breath. It passed, it shipped, and it still does not land for
  the one person using it.
  **That is the signal, not the bug.** The 08-05 fix made the affordance
  CONSTANT; Aaron is asking for it to be LOUD. Those are different axes and I
  optimised the wrong one. The next lever was already filed that day and is
  still the right one: **an explicit TAP TO PLAY chip, or motion with real
  travel** (the "hover up and down" he actually asked for both times), rather
  than more glow.
  A second report of a closed item means the close was wrong. Reopened.

### Filed 2026-08-07 out of the IN-GAME install prompt (B2b)

Built and NOT merged: `docs/play/install.js`, the coach's first word, and the
logo as a permanent handle. Walkthrough published for Aaron before it ships.
Three things it leaves open, filed here rather than left in a chat reply:

- [ ] **DECISION · the hint pill wording.** It currently reads
  **"Add to home screen"**, which is plain, correct and a bit flat next to the
  rest of the game's voice. This is the second-most-seen string on the title
  screen after the Daily 5 stamp. One word from Aaron and it changes; it is a
  taste call, not a default.

- [x] ~~**The coach's welcome fires ONCE PER PHONE, forever.**~~ **RULED
  2026-08-11, Aaron picked the profile reset: "new profile = new hello."**
  The welcome key moves INTO the per-player profile envelope when B8's store
  builds (the store is ruled and unblocked), so a fresh profile on the same
  phone gets greeted and a veteran never is. Nothing to build until B8; filed
  as part of its schema. ~~Old text:~~ `bk_install_seen`
  plus the coach's own `bk_coach_seen`. That is right for something that would
  otherwise be a nag, and it means **Aaron cannot re-test it on his own phone
  without clearing site data.** Worth knowing before he tries. If a "replay the
  coach" control is wanted, `BKCoach.replay` already exists in the Control Room
  and could clear this key too — small job, not done.

- [ ] **THIS IS A SLICE OF B7, NOT B7.** What shipped: the coach can now speak
  on a menu screen, and a tip can carry one primary action button. What did NOT
  ship: the first-run guide that actually teaches the game. B7 stays where it
  is on Track B and is not closed by this.

- **B7's DRILL LAYOUT IS RULED (Aaron, 08-10: "Okay this works, let's live
  it").** The shape the build targets: **11 drills + 3 parked builds + 2 coach
  trays**, with the rail/check-off/jump/redo interaction, action gating and the
  reserved coach lane. The one home for the layout is
  `design/COACH-BOARD-2026-08-10.md` (his filing plus the ruled round-two
  layout); the playable reference is the Drill Room artifact. What this does
  NOT unblock: the coach block's one open question (how much the Coach may say
  before he is annoying) is still Aaron's, and B5's two drill defects are the
  layout's prerequisites and stay open on this track.

### Filed 2026-08-07 out of the add-to-home-screen work

- [ ] **TWO MORE ICON SHORTCUTS, owed when their features land.** Aaron,
  2026-08-07: *"once we build quick run, that and online play should be
  shortcuts too."* Agreed, and the plumbing is already done: the manifest has a
  `shortcuts` array and `firstScreenDeepLink()` in `game.js` is the one place
  that reads `?go=`, so each new one is a case in that function plus an entry in
  the manifest plus a check in `tools/pwa-check.mjs`.
  - **`?go=quick` → Quick Run.** Owed by **B9**. Do it in the same commit as
    Quick Run itself, not afterwards.
  - **`?go=play` → online play / join a game.** Owed by **B3**, and it wants
    thinking about rather than copying: the useful shortcut is probably *join
    the game somebody invited me to*, which is the invite link's job, not a
    generic "go to multiplayer". Settle that when B3 is designed.
  **THE RULE THIS ESTABLISHES, and it already caught one today:** a manifest
  shortcut whose url the app ignores is a lie shipped in config. A shortcut and
  the deep link it points at ship together or neither ships. `pwa-check.mjs`
  drives every shortcut url for real, so a broken one fails the harness rather
  than failing on somebody's phone.
  **Android only, and that is Apple's call not ours.** iOS does not support
  manifest shortcuts for home-screen web apps. The iPhone equivalent is adding
  the deep link as its OWN second icon, which works today and is written up for
  players in the install guide (PLACES.md).

- [ ] **D24 · THE INSTALL CARD FAILED ITS FIRST REAL TESTER, twice in one
  screen.** FILED 2026-08-10, from tester session #1 (Malik, Aaron's friend,
  BUILD.md 6c). Two distinct failures on the "Put it on your phone" card:
  - **The concept never landed.** Malik read "Add to Home Screen" and did not
    understand it, because to him *app means go to the App Store*. The card
    explains the HOW (three steps) without ever saying the WHAT: that this
    saves the game to your phone like an app, no store, no download. The one
    line that says so ("Nothing downloads... the same game, wearing an icon")
    is the FOOTNOTE, in the smallest type on the card. The fix direction when
    we act: lead with the what, in his vocabulary ("Get the app icon · without
    the App Store"), then the steps.
  - **Step 1 is wrong on phones like his.** The card says *"Tap the Share
    button at the bottom of the screen"*, but iOS Safari's compact address bar
    hides Share behind an **ellipsis (...) menu first** (screenshot in the
    session record). Malik's exact words: *"I don't have a share button."* An
    instruction that skips a step the phone shows is worse than no
    instruction, it makes the reader feel wrong. Fix direction: cover both
    paths ("see ... instead? Tap it first"), or detect nothing and describe
    both, since layout depends on a Safari setting we cannot read.
  Aaron's aside, same message, already answered in the docs: the App Store
  question (native wrapper, its costs) is written up in `APP-AND-MONEY.md`;
  nothing new filed there.

- [ ] **DECISION · what the icon is called on a home screen.** `short_name` is
  currently `"Ball Knowledge"`, and iOS truncates the label at roughly twelve
  characters, so it will read **"Ball Knowle…"** under the icon. This is
  branding on the most-seen surface the game has, so it is Aaron's call, not a
  default. Options: **"Ball Knowledge"** (accept the ellipsis), **"Ball IQ"**,
  **"BALL KNOW"**, or **"Hoops IQ"**. One word from Aaron and it is a one-line
  change in `manifest.webmanifest` plus `apple-mobile-web-app-title`.
  *Claude's pick: "Ball IQ" — it survives the truncation, it is what the game is
  about, and the full name still shows in the install sheet.*

- [ ] **DECISION · service worker: yes, no, or later.** Without one, iOS
  Add-to-Home-Screen works completely (it only needs the apple meta tags), and
  Chrome offers "Add to Home screen" from its menu but not the richer install
  prompt. **It was deliberately NOT added today**, because a service worker on a
  GitHub Pages site is the classic way to serve a stale `game.js` forever to
  somebody who already visited, and this is about to go to twenty people whose
  caches nobody can clear. If it lands it needs a versioned cache and a skip-
  waiting path, which is its own job with its own harness. It also overlaps
  **B4**, handling the sleeping server gracefully — an offline shell is one
  honest answer to that — so the sensible time to decide is when B4 is built,
  not now.

## THE ORDER, TWO TRACKS — everything left before the twenty

**Written 2026-08-07, at Aaron's ask:** *"I want to keep the list of what I
wanted to do to get the game to the 20. But organize THAT list by what should be
done first."*

**This is the plan. Nothing else in the repo is.** `RESEARCH-BACKLOG.md` is a
queue of every research job ever noticed, most of it post-launch; `BUILD.md`
§ 3–4 is the superseded July board. If a job is between here and the twenty
friends, its position is decided in this section and nowhere else.

**Why two tracks and not one list.** Aaron's own ruling: the build items *"start
the moment the card target is hit, running ALONGSIDE the research, not after
it."* They genuinely do run at once, because Track A is mostly reading and Track
B is mostly code. A single numbered list would imply one has to finish first,
which is the thing he explicitly said not to do.

**How to read it.** Each track is in order, top to bottom. Do the top open item
on each. **Aaron's total input across both tracks is two pastes and four
one-line rulings** — everything else is Claude's, which is what makes them
parallel rather than interleaved.

---

### AARON'S LIST, 2026-08-17 — ten items, filed the turn they arrived

He sent ten things at once and asked for them one at a time, opening with
*"over the past 20 or so back and Forths we have had, have you been tracking
in all the correct docs?... I'm sad I have to keep reminding you of this."*
He was right to ask: `next.py` was pointing at B5c, a row marked BUILT AND
MERGED that nobody had struck through, so the plan's own answer to "what is
next" was a finished job. B5c and B16 struck the same turn.

This block is the INDEX. Each item names its home and its status; the work
itself lives on the tracks below, never here.

| # | his ask | home | status |
|---|---|---|---|
| 1 | docs up to date across the board | this block + the fixes it names | IN HAND 08-17 |
| 2 | the turn is still unclear · "YOUR TURN!" + a player menu + CPU trash talk | **B17** (turn clarity) and **B13** (trash talk, shape now RULED) | DIRECTION APPROVED 08-18 with four rulings: **duplicate squad names BLOCKED at setup** · the waiting dock is the quiet **"They're up" strip** · **the lights-down must leave the floor fully lit** (*"the player needs to watch the board, they have to be watching what's moved"*) · **the free moves open the turn in the dock**, with a live count and DONE as the only door to the action (his ask: *"how are we alerting or informing the player of their free moves before main action"*). Mockup v2 delivered with all four in; build waits on his read |
| 3 | do we need research to design engaging gameplay | RESEARCH-BACKLOG **22aj** | OPEN, needs his go |
| 4 | the whole game lacks smoothness, transitions and sounds are abrupt | **B18**, new | OPEN |
| 5 | menus should lead you like the Apple Store checkout does | **B19**, new | OPEN, he offered a video |
| 6 | the toss-up buzzer should not be on top | **B20**, new, comparison owed | OPEN |
| 7 | Quick Run, with setup ideas from me | **B9** | OPEN, ideas owed |
| 8 | get the coach right with all of this | **B7** | OPEN · **RE-ORDERED 08-18, his call: the coach rewrite goes LAST**, after turn clarity and the rest of this list |
| 9 | **Method B is the method.** Archive the old one, remove the switch | **B16** + DESIGN.md | RULED, build owed |
| 10 | solidify the defensive tax, and verify you understand it first | DESIGN § 4 | **RULED AND BUILT 08-18.** He confirmed rules 1+2 and dropped the momentum tax himself (*"movement will be movement and the cards are the price that's all"*), which dissolved the centers collision without a fix. THE ONE DEFENSE shipped to the branch: all-eight guarding, corner duels one step cheaper, two-man lanes closed, tiles wear their card's colour, CPU never taps a wall, picker retired, rulebook + coach copy rewritten, 11-check defense-check sabotage-proved twice. STILL OWED inside it: the skill escape hatch when ratings land, and a Coach drill teaching the corner discount (the cross drill teaches the duel, not yet the pricing ladder) |

**HIS PART 2, 08-18, verbatim**: the CPU *"should absolutely understand these
rules once we have nailed them down"* · tiles: *"the tiles should be all there
when free movement and show one less during crossovers, it should just be
clear what tiles you can move to and their difficulty... it shouldn't be a
math problem for the gamer... The math problem for the gamer is winning the
game not how to play"* (that sentence is the STANDARD for every rules
surface, worth keeping next to the momentum-tax lesson) · and my phrase "no
gate on the discount" meant nothing to him, which is its own lesson: gate,
harness, tier are OUR words, not his.

**HIS PART 1, 08-18, verbatim wants** (he flagged my read-back as too
jargon-heavy, so the re-explanation owed with part 2 must be in plain words):
1. *"head on is full price and difficult to cross over, diagonals are
   cheaper (this exists already for shots right?)"* · his question in the
   brackets is real and owed an answer with part 2.
2. *"lanes where two people are guarding should be closed (unless you have a
   highly skilled ball handler, this is when we inject skills and stats based
   basketball into the game)"* · NOTE: this takes the lane-closing rule that
   today lives only in the ONE-ON-ONE setting and makes it part of the main
   rule, with a skill-based escape hatch. The escape hatch is the first place
   player RATINGS would gate an action rather than bend a meter.
3. *"I want a floor analysis given the above and a HONEST TRUE analysis of if
   the floor is too small for all of those rules, do we need to make the floor
   bigger or do we need to make defense less restrictive"* · measured, not
   argued: how much of the floor each rule set eats, on the real board, with
   the Method B defensive shapes placed by the game's own tables. (First
   filing said 13x7; the NBA board is 15x8, the smaller grid is only the
   pre-game default. Caught by the model-vs-game validation pass.)
4. *"this needs to be in drills and rulebook once decided"* · teaching ships
   with the rule, not after it.
| 11 | the bonus counts up to 30 | shipped | **FIXED 08-17** |
| 12 | ON FIRE must lose its black background | shipped | **FIXED 08-17**, it was showing everywhere, not only in the Daily Five |
| 13 | ~~bring the momentum tax back as FEWER TILES on a crossover~~ | DESIGN § 12b | **DROPPED BY HIM 08-18, never built.** His centers question killed his own design in a day: *"I think we just drop the momentum tax."* Movement is movement; the cards are the price |

**Item 9 is a ruling and gets recorded as one.** Aaron, 08-17: *"not a small
thing but we are goin with method B (store the old method in our history so we
know what it was if we ever have to build it again, and remove the option to
switch)."* So the prototype flag stops being a prototype: Method B becomes the
game's possession model, the classic branch is ARCHIVED rather than deleted
(quarantine-never-delete applies to code we might have to rebuild), and
Settings loses the toggle. This unblocks the coach rewrite that B16 had gated
on the friend playtest: he has ruled without waiting for it.

**Item 2 rules the SHAPE of trash talk**, which B13 had left open: *"for same
phone local or online play we just have prefixed emojis and prefixed things to
say so we can avoid troubling chats and that's what we will release as trash
talk for now."* So v1 is a closed set of canned lines and emoji, never free
text, with the CPU barking its own ("what's the holdup!?", "you scared!?") and
an off switch. That answers the moderation cost that put chat last on B13.

**Item 10 is deliberately not started.** He asked me to verify I understand the
rule before touching it, and I have not read it back to him yet.

---

### TRACK A · DATA — to 1,000 dealable, diverse, NBA + WNBA

Measured 2026-08-07 (`tools/gate-blockers.py`, `tools/diversity.py`):

```
 317   dealable and in scope TODAY
 961   in-scope cards that exist in the bank at all
 607   ceiling if EVERY readable card gets verified
 393   cards that must therefore be NEWLY WRITTEN
```

**The number that sets this order: verification is the road to 607, not to
1,000.** Any plan that says "finish verifying, then start writing" is a dead end
with a number attached. So the writing starts at A5, not at the end.

| # | item | who | why here |
|---|---|---|---|
| ~~**A1**~~ | ~~**V29 Run B — the licensing read.**~~ **DONE 2026-08-07.** 102 quoted rows across 94 documents, filed UNPROVEN. | DONE | It gated A6 and A7 and it cleared them: aggregating at scale is a SUBSTITUTION test, not a volume test, so V32 survives in the shape it was already written in. The prove pass on the quotes is now A3c. | Aaron's own sequencing call: *"so we don't waste time on research only to find out we can't use it."* It gates A6 and A7. It costs one paste and then runs while everything below happens, so it goes first for the same reason you put the kettle on first. |
| ~~**A2**~~ | ~~**The slang ruling (V36).**~~ **RULED 2026-08-11, and it is option 1 with a second leg.** Aaron: *"if we know the definition of the words, just use the dictionary and match the word to whatever it is in the real sources."* So a vocabulary card needs BOTH: a lexicographic source for what the word means, and a real basketball source using it that way. Full wording in RESEARCH-BACKLOG V36. | **DONE** | Cheapest unblock in the project. Worth more than its 20 cards because it sets the precedent for a whole class, and that class recurs on every vocabulary pass. Do it in the same sitting as A1. |
| ~~**A3b**~~ | ~~**Reword the 20 stale-flagged cards that are otherwise dealable (V25).**~~ **DONE 2026-08-09, and the projected gain was wrong in one honest way: the 19 (measured, not 20) were all INSIDE their 180-day window, so the pool did not grow today; what changed is they no longer fall out of it next February, ever.** Three-way sort per V25: 2 flags cleared (frozen events), 2 record clauses cut to notes, 15 anchored `Through the <season>` with `anchor` set and the flag KEPT. `anchored_unreviewed` still 0; gate green. Details in V25. | Claude | **The best no-fetch job on the board: 19 cards de-volatilised with zero requests to anybody's server.** Pairs with V27, the same rows read for the opposite error. |
| **A3c** | **The prove pass on V29 Run B (V42).** Re-read all 30 quoted `terms` clauses at their URLs, starting with the 9 that came back honestly flagged as not verbatim. | Claude | **Nothing in BUILD.md § 5b.1a-2 becomes policy until this runs**, and the word UNPROVEN stays on it until then. Terms pages change; a research tool can quote a cached copy. Court opinions do not change, so the 72 `law` rows do not need it. |
| **A3d** | **The publisher terms read (V45): 13 OF 17 READ on Aaron's Go, 08-11.** What remains: **`hoophall.com` needs AARON's one-minute hand read** (its terms page is JS-rendered, unreadable to every fetcher tried; 10 citations wait on it) · cnn.com, newsnationnow.com and ashof.org are mine, a few citations each. Best news of the run: `springfield.edu` came back GREEN, no terms exist at all, 14 citations clear. Sharpest clause found: CBS/Paramount's explicit "store in a database" prohibition; Disney's automated-collection clause covers espn + andscape. Verdicts filed in RESEARCH-BACKLOG V45. | Aaron (hoophall) + Claude (the last three) | *"Publishers do not have anti-database clauses"* was a structural argument, and the measurement is now 13/17 done: some publishers DO have them, named above, which is exactly why the read happened before the pre-1980 write A5. | |
| **A3** | **The era lookup pass.** 321 of 1,526 facts carry no era tag; 43 of them are in the dealable pool. `era-tag-lookups.json` is down to 3 rows, so this is tagging, not research. | Claude | Half of Gate 1 is diversity, and era is the only dimension still thin. This is the cheap half of that fix — no reading, no sourcing, just tags. The rule is already settled (D1 / 22q), so nothing is blocked. |
| **A4** | **Block D — 90 cards that have exactly one Tier 2 source and need ONE more publisher.** | Claude | Best cards-per-hour on the board. Ninety cards is nearly a quarter of the gap and every one already has a real source; the job is finding a second publisher, not starting from nothing. |
| **A5** | **Write the pre-1980 NBA cards, aimed by `diversity.py --thin`.** The season spine (V37) returned **609 facts across 77 seasons and exactly one has been used.** | Claude | **The only item that moves both halves of Gate 1 at once** — it adds cards AND fills the six thin eras (1930s, 1950s, 1960s, 1970s, nba-1940s, nba-1950s). The facts are already on disk with Tier 1 sources attached, so these are the cheapest NEW cards available without a single fetch. |
| **A6** | **V32 — mine the 158 Tier 1 pages we already trust and cite exactly once.** Gated on A1. | Claude *(A1 first)* | Cheapest acquisition that exists: page found, tier ruled, bytes cached, only extraction left. A leaderboard cited once for one card holds dozens. Held behind A1 because it is precisely the activity the licensing read is about. |
| **A7** | **Block E — 198 Wikipedia-only cards: follow the footnote and cite what IT cites.** (V15 / V35) | Claude | The single biggest block, and honest work rather than clever work. Below A5 because writing new cards from good sources beats re-sourcing old ones when the ceiling is 607. |
| **A8** | **V22 link resolution as a SCRIPT, then V20 multi-league emit, then V19's remaining `l:any`.** | Claude | Mechanical multipliers. Every pass above assumes links resolve, and two cards already rested on a typo'd URL nothing caught. V20 blocks V19; V19 gates how far V13 can reach. |
| **A9** | **V26 twins (55 pairs) + V17 second sources + `posture` (V44).** | Claude | A pool with twins in it is smaller than it counts itself as, and Gate 1 is a count. **V44 rides along free**: `posture` is a second axis beside `tier` — tier asks how reliable, posture asks whether we are welcome — and the rule it buys is a tie-break, *where two sources prove a fact equally well, prefer the permissive one*. V17 already touches exactly these rows. |
| **A10** | **Blocks F and G — 37 weak-tier cards, then 317 with no URL at all.** | Claude | Last of the verification blocks on purpose: G is a FINDING job, not a reading one, and it is the most expensive card on the board. |
| **A11** | **V39's pre-1997 women's half — attested claims for "Before the W".** | Claude | Needed before any "Before the W" card can merge, because that material is thinly documented by design. **Its Black Fives and streetball half is post-launch** and does not belong on this track. |
| **A12** | **V25 reword the stale-able · V27 the `goes_stale` audit · V40 the rejection vocabulary.** | Claude | Upkeep. V25 is the good kind — it deletes future work permanently rather than doing it faster. None of the three blocks launch; they stop the bank rotting after it. |

**Also on Aaron's desk, whenever he is answering things:** ~~V18, V21b, V23~~
**all three RULED and executed 2026-08-12** (tiers applied to the register ·
five superlatives anchored · the free-throw card confirmed already reworded).
What remains his on this track: **the hoophall.com one-minute hand read** (A3d)
and nothing else.

---

### TRACK B · BUILD — the 28 items

**Counts live in `python3 tools/next.py`, never here** (a hand-written count
in this header went stale within two days of being written on 08-06; the tool
read 8 done · 10 open when this line was last touched, 08-12, after B15 the
reveal build joined and B5 closed).
Ordered by V0's own test: *will this make twenty people play it twice and show it
to someone else?*

| # | item | why here |
|---|---|---|
| ~~**B1**~~ | ~~**Merge the Daily Five.**~~ **DONE — and it was already done when this table was written.** `daily.js` is on `origin/main`, byte-identical to the branch; it went in with the merge on 08-07. This row said "NOT MERGED" because the note it was built from said so, and I did not check `git cat-file` before writing it. |
| ~~**B2**~~ | ~~**Add to home screen.**~~ **DONE 2026-08-07.** `docs/play/manifest.webmanifest` (standalone, scoped to `/play/`, ground `#100d0b`), the iOS meta tags Safari needs instead of the manifest, a purpose-built maskable icon, and an icon shortcut straight to the Daily Five. 24 checks in `tools/pwa-check.mjs`, five sabotages proven. | The one Aaron named by name. The difference between a link somebody loses in a group chat and an icon on their phone. |
| ~~**B2b**~~ | ~~**The in-game install prompt.**~~ **DONE 2026-08-07, merged.** | The manifest made the game installable; this is the half that makes anyone notice. The coach speaks once with a button, the logo is the permanent handle, and the Coldest Call spotlight dims the world and cuts the logo out of the dim. Goes silent once installed, and comes back if the icon is deleted. 50 checks, six sabotages proven. |
| ~~**B3**~~ | ~~**An invite link that just works.**~~ **DONE 2026-08-08.** `inviteUrl()` + `shareInvite()` + the `?join=CODE&k=` deep link, 17 checks. **The second half was RULED, not built:** the access gate stays ON with the pass riding in the link, Aaron's call, so `gateShow({k:'rejoin'},...)` is still live at `game.js:765` on purpose. | **The biggest drop-off risk at launch.** Twenty people who owe you nothing will not fight an access code. Today `gateShow({k:'rejoin'})` is still live. Every hour spent above this is an hour spent improving a game some of them will never reach. |
| ~~**B4**~~ | ~~**Handle the sleeping server gracefully · wake lock.**~~ **DONE 2026-08-08.** `navigator.wakeLock` re-acquired on `visibilitychange`, 12 checks; and `netPoke()` starts warming the server the moment anyone opens Online, so it heats while they read (`game.js:6465`). | Both are "the game appeared broken" bugs. A cold server on the first tap loses a tester permanently, and a screen that sleeps mid-game ends the session. Cheap insurance on B3's traffic. |
| ~~**B5**~~ | ~~**The playthrough defects, worst first.**~~ **ALL SIX ACCOUNTED FOR, 08-11:** ~~the coach card covering the board~~ (it was covering the CONFIRM row, not tiles · measured at 14px) · ~~drills refusing off-drill moves~~ (greyed, and the tap still explains itself) · ~~the three rulebook items~~ (own-line drill button · accordion · the drill audit: 16 topics, 7 had drills, 3 added, 6 ruled not to need one) · ~~what survives a back button~~ · and the sixth, pack rarities being invisible, **got its taste ruling the same evening: "The rarity mock is good" · it is now the B15 reveal build below**, not an open defect here. | **These came from Aaron actually playing**, which makes them the highest-signal defects in the repo. Broken things in shipped features beat new features. |
| ~~**B5b**~~ | ~~**The three cheap Daily Five defects.**~~ **DONE.** `daily.js` calls `BKAudio` in three places (D3), the hover highlight is behind `@media (hover:hover)` (D1), and the copy landed (D2). | **Under an hour for all three.** D3 is pure wiring — `daily.js` has ZERO `BKAudio` calls and `audio.js` already synthesises swish, brick and buzzer, so nothing needs sourcing. D1 is one `@media (hover:hover)`. D2 is a line of copy Aaron owes, because the come-back mechanism it promises is incompatible with everyone getting the same ten. |
| ~~**B5c**~~ | **✅ BUILT 2026-08-16, his word same day ("go ahead and push the daily 5 build changes"): the theatre is IN the real daily.js · the flight, the rings, the carom, the slam in the LINES voice, Defend-the-Floor round 2 on the dusk court, sourced sound windows with synth fallback, three ending tiers with ROOF OFF on the game's own fireslam. 15 new mechanism checks pass (tools/theatre-port-check.mjs) + all daily harnesses re-paced and green. Comparison: <https://claude.ai/code/artifact/035d493d-53fa-401b-bd6d-b9d33a4f5f16> · BUILD § 7 08-16 has the full story, including his Art Draft picks (P2 · Q4 · J8) wired the same block. HARDENED same evening by the 61-agent double-check he asked for: 28 findings, 25 fixed in the follow-up commit (the tap-eating confetti overlay was the big one), plus two FILED here: (1) the r1-to-r2 floor flip is a hard cut, background-image cannot transition · a crossfade needs a second layered element, polish, build with the backdrop treatment he picks from the Daily Floor board; (2) PRE-EXISTING, not from B5c: the card-advance timer keeps dealing and arming clocks after the player walks out mid-answer, so an abandoned run burns down while away · predates the theatre, same chain in the old code, needs its own small design call (pause the deal on hidden? bank the run?); (3) FILED 08-16, found while verifying the rim seat: on DESKTOP/LANDSCAPE viewports the world's cover-crop of the portrait art pushes the painted rim to the bottom of the screen and BEHIND the answer sheet (measured at 1440x900: rim y 864 of 900), so the flight disappears under the card · pre-existing on live main since the world merged, phone-first is why nobody saw it · needs a design call on how THE WORLD behaves on wide screens (cap the stage? letterbox? re-anchor?), Aaron's call. And one RULED-equivalent: under reduce-motion the pts pop and swish rings do not render, matching the game's own confetti-off-under-reduce-motion pattern; the marks and checks still land. MERGED to main 08-16 (34b066a), his order after the timer fix: "I LOVE your new build... if you fix that, then merge." The world redesign (T5+T6, the container gone, spots and flight on the painted court) merged with it, LIVE for the twenty. HIS LAST TWO CATCHES OF THE NIGHT ("a bit off... also a bit low") CLOSED, AND MERGED to main 08-16 late on his order ("merge that stuff before my last message"), together with the rim first pass and the coach MB-mute fix: rim re-measured to the ring's true centre and top plane on 8x grids (the first calibration had read the ring's right edge), iron contacts now seat the ball a radius up on the ring, and the .44s door animation no longer feeds a mid-pan rect to the spot/ring pinning (dvSettle waits it out) · three new harness assertions hold the seat · BUILD § 7 08-16 rim entry. AND THE FIVE RESPACED, on the branch AWAITING HIS EYE (his ask: "just try and let me see what you come up with"): round-1 spots re-anchored to the painted court's own geography (the old layup and elbow chips sat 58px apart at 62px wide, and three of five floated on the fence above the floor, which only begins 52% down the art), the sheet clamp grew a collision pass after 390x667 stacked layup on the logo, RE-RULED same night when v1 lifted the logo above the layup (Aaron: "too much of a sacrifice moving a half court shot above a layup") so the clamp now resolves deep-to-shallow and court order can never invert, two new no-overlap harness gates at both heights, before/after at <https://claude.ai/code/artifact/98b35c77-89ca-452f-ab9b-90b369df4862> · stops untouched on purpose.** Original ruling, kept: **GIVE THE DAILY FIVE SOME THEATRE. RULED 2026-08-11, Aaron: "we are good on the changes" · SHIP IT.** Two rulings rode along: a MISS never reveals the answer (the reveal lives in a future per-player film room, see the B8 store below), and the miss line stays cold. Sample: <https://claude.ai/code/artifact/efc4fa3d-e5ac-4184-9bf8-34f9f1aa3809> · the annotation used to sit BETWEEN the id and the cell divider, which broke next.py's row parser and made this row INVISIBLE to the plan for two days (found 08-11: the tool said 9 open, the table held 10)** — D4, D5, D6, D7, and D8 with them.** Right/wrong is a whisper; there is no shot; defence does not announce itself; the stamp still does not read as tappable. | Aaron listed these as four things and they are one: **the mode REPORTS results, it does not stage them.** A caption fading in is a status message; making a shot should be an event. **The shot animation is the SAME job as the main game's unbuilt shot effects** (arc trail, swish burst, rim rattle) — build it once, drive it from both, or the two screens end up looking like different games. D8 is a reopened item: the 08-05 fix made the affordance CONSTANT and Aaron is asking for LOUD, which is a different axis. |
| ~~**B16**~~ | **✅ BUILT 2026-08-16, behind the flag · Aaron: "love it, lets build it! we have to get my friend playing!" Settings > Prototype > Method B, full-court local + CPU games; flag off = the shipped game, proven by the harness's regression section, seven dedicated checks (`tools/methodb-check.mjs`, 35 checks total, all pass). Save point: main at `b1f1abf`. HIS FIRST REAL PLAYTEST, 08-16 late, three catches, all answered on the branch: (1) "the switch didn't work / nothing happened differently" · no engine defect found on any driven path (the switch flips the live flag, NBA local latches, the ritual renders), the defect was SILENCE, FIXED: the latch now slams METHOD B ON at the tip, a flag that is on but disqualified says WHY it sleeps (full-court 5v5 local only), and a human's free-setup beat gets the big callout instead of only the banner strip; (2) "don't like the layout for picking the setup" · RE-RULED to his spec and BUILT: the picker is a PEEKING CAROUSEL at the bottom, one card per setup with a court diagram drawn FROM the placement table itself, tap = live preview of the shape on the board (re-tap browses without drift, guarded by a baseline restore), RUN IT confirms · methodb-check rewritten to the carousel with four new preview assertions, all green; (3) "shouldn't there be a pick after the tip off too? ...ohh no that's wrong, there isn't, right?" · CONFIRMED: none today by design, the ritual belongs to dead balls and the tip is a live ball (same law as no-reset on steals) · the open option if the playtest wants one stays filed below as the jump-ball ritual, best shape: a pregame HUDDLE pick before the tip, which would also teach the menus calmly before the first dead ball. Still open inside the row: online support, a CPU setup brain (it currently skips its free setup), jump-ball ritual (no pick after the tip today, CONFIRMED intended 08-16; the pregame-huddle variant is the candidate if the playtest asks), the two toggles await the friend playtest, COACH x DAILY VERIFIED 08-16 at his ask (three suites green, live probe: card topmost by hit-test, clock held and resumed; a six-area agent trace confirmed stacking, clock-hold, dodge, anchors and veil, and caught ONE real bug, FIXED same hour: MB.game latches per game with no reset on exit, so one Method B game left the coach MUTED on the daily and everywhere after · MBPROTO now checks the game screen is actually up, proven by a new methodb-check assertion: the coach SPEAKS on the daily after an MB game); and THE COACH'S METHOD B PASS (Aaron 08-16: "How does the coach work with the new gameplay? Do we need to do a new brainstorm/braindump for the gym drills?" · answer: the coach is silenced in prototype games on purpose, and IF the playtest rules Method B in, coach.js scripts, the COACH-TOURS mapping and the seven gym drills all teach the OLD possession and need a rewrite braindump · that work is gated on the ruling, so it is filed here and not started). Full story: BUILD § 7, 08-16.** Original scope, kept for the record: **METHOD B, PLAYABLE · the possession rework behind the staging flag, so Aaron and his friend can feel it before anything is ruled.** Scope: the inbound ritual (defense picks its setup first and visibly, offense picks seeing it; the lists are his accepted 08-16 picks, "nahh I liked your recs": MAN · 2-3 ZONE · BOX-AND-ONE and HORNS · FIVE-OUT · FLOPPY, contextual joiners per the Up the Floor menu table) · full free off-ball setup · the slide re-timed to after-setup-before-action · no reset on live-ball turnovers · and BOTH open numbers as in-flag toggles: free movement (1 tile each vs full range each) and the slide (1-2 tiles vs full movement by role, one defender either way). Coach, tours and drills go SILENT in prototype mode behind a plain "prototype" banner, because teaching rules that may not survive the playtest writes bad habits and bad docs. REVERT ARCHITECTURE, his ask: a tagged save point on main before the flag ships, flag-off as the instant undo, and the prototype merges to main only as dormant flagged code so the live game never changes for the twenty. MERGED dormant 08-16 (34b066a): the flag ships OFF, the live game plays the shipped rules until someone flips Settings > Prototype. Skeleton can start before 22ai returns; the play lists slot in when he picks them. ADDED TO SCOPE 08-16, his catch: the offense's setup list is CONTEXTUAL BY DEAD-BALL SPOT, exactly as the research split it: baseline dead ball offers the BLOB shapes (Box, 4-Low), sideline offers the SLOB shapes (Zipper), made basket offers the backcourt inbound; the sail-out mechanic that feeds this ALREADY SHIPS (sailPass + inbound(deadTile), DESIGN § 1 line 31). Timeouts join the ritual later as player-called dead balls (BUILD § 5 item). | His words, 08-13: "I want to implement this in a real game so I can see how gameplay would be and I want to get my friend to play this new version... with the designed ability to go back to the old version if this is bad." The friend playtest IS the ruling mechanism for the two open numbers. |
| **B17** | **THE FEEL BUILD · Aaron ruled the Flatness Board in pieces, 08-16 late.** BUILT ON THE BRANCH 08-16 late, his "Okay let's go": the TURN TRAY stands (the beat as a ticking checklist under the HUD, SETUPS to BALL IN to FREE MOVES with the live count to SLIDE to ACTION; the banner steps down a row while it stands) and the PLAYER MENU prices the carrier's real options from the engine's own functions (zoneOf prices the shot, mbOpenCount reads coverage, the duel warning is the tile grammar), both MB-flag-only with the classic branch byte-identical, two new harness gates (tray-at-ACTION, menu-prices), 43 methodb checks green · zone-glow on option highlight is the one piece deferred, filed here · THE SOUND SHEET is written (design/SOUND-SHEET.md): three layers on the shipped measured-window player, the 30-bark name-free announcer bank with its trigger map, five state-driven chants, the game-point hush, and the three voice options with ARON-RECORDS-IT recommended for v1, his voice pick pending. Original rulings kept: the TURN TRAY ("love the tray") · the PLAYER ACTION MENU ("love the player menu"; wheel-vs-menu comparison delivered <https://claude.ai/code/artifact/fb6283ae-ee6b-47ee-97aa-efe961db2aba>, my rec was the MENU and HE PICKED THE MENU same night: "I like the menu too" · builds with the zone-glow steal) · THE THEATRE PORT to the main game ("C. yes", and expanded same night: "I like porting in the sounds from Daily 5 and I wonder how real we can get with crowd and announcer sounds? Throughout the game, even chants and stuff too" · the sound ceiling answered in chat 08-16: layered crowd beds + event barks + state-driven chants are all buildable on the shipped measured-window system; a real ANNOUNCER voice needs his taste ruling on generated voice, the documented Firefly capability, or his own recorded reads) · HALFTIME + GAME POINT ("halftime and closing are yes"). PENDING HIS READ: stakes-on-the-tile (example delivered, frame C on the same board) · RUNS (explained in chat 08-16, his call) · TEAM TURNS (explained vs Method B in chat 08-16, decision waits for the friend playtest). ALSO HIS: the BANNER BOOK, every top-strip sentence with keep/trim/cut verdicts, awaiting his red pen <https://claude.ai/code/artifact/5faef9fd-46e8-4943-9562-6bc7eeeca364> · his own catch (M1, "in the open") already marked TRIM. Build order per the board: tray + menu behind the Method B flag first, then the theatre port, then halftime/game point. Nothing here starts until the wheel-vs-menu pick lands. | His words: "the overall gameplay still feels really flat... this has to be brainstormed more." The board named four causes (silent turn · nothing accumulates · invisible stakes · the board does not answer back); these are the treatments he ruled in. |
| ~~**B6**~~ | ~~**In-game feedback button.**~~ **DONE 2026-08-09.** `docs/play/feedback.js`, three entry points (pause menu above Exit, the final buzzer, Settings), three kinds rather than a form, and fifteen fields of context SHOWN before anything is sent. Sends by `navigator.share` so it lands in the tester's own messages to Aaron: no endpoint to stand up and none to go down. Stored to localStorage before the share sheet opens, so a dismissed sheet loses nothing. 27 checks in `tools/feedback-check.mjs`, three sabotages proved. | The entire point of the twenty is feedback. Without this it happens in a group chat, out of context, or not at all. Small, and it makes everything after it measurable. |
| **B7** | **The coach as a first-run guide.** *(Aaron moved this INTO Track B on 08-07: "I noticed that in the future track you listed drills as tutorial, I wanted that in track B.")* | **Somebody who does not understand the game on first open never plays twice at all**, which outranks every progression feature under it. `coach.js` already has a coach card, a step model and drill scripting, so it extends something real rather than starting a system. It is the biggest item on this track by a distance, so it does NOT get started inside a bug-fix pass. Full spec is in "Still open on things already built" below. |
| **B8** | **Cards remember you between games · play logging. THE STORE IS PROPOSED (08-11), Aaron's read pending before any build.** One versioned envelope per device profile (`bk_player`: id, name, cards{qid: seen/right/wrong/lastAt}, compact per-game log, daily history, settings), migrating today's scattered keys under one shim. The Daily Five film-room reveal, the gym progression and play logging all hang off this ONE store, which is why it gets designed once, together · his words: "This needs to be done right and across the board." Full proposal in chat 08-11. **RULED same day, Aaron: "Yes" · a player = a device profile, no login, for the twenty. The store is unblocked and B8 can build.** | Progression is the second-play reason, and logging is how you find out what actually happened in those twenty games. Paired because they share storage. |
| **B9** | **Quick Run.** **THE TOP-SLOT QUESTION IS RULED. Aaron, 2026-08-11: "today slot stays quick run."** So the TODAY square is Quick Run and the question is closed; both opponents stay in the COMPETE rolodex and Online grows by PRESENCE, not position. The reasoning behind the recommendation he took, kept because it governs the NEXT top-slot argument as much as this one: It is the express lane (CPU under the hood, zero choices, always opens); Online at top is a door that opens onto an empty room whenever no friend is free, which with twenty players is most taps. Online grows by PRESENCE (badges, invite links, later async "your move" notices that can earn a top strip because a waiting move always works), not by position. Both opponents already live in the COMPETE rolodex, so promoting either duplicates a door. | A game to 11 is the barrier Aaron named himself. A four-minute mode is the answer, and it costs far less than the alternatives. |
| **B10** | **The heat sound.** | The one missing third of a shipped feature — V0 asked for popup, sound and readout; two of three landed. `heatCard()` plays nothing. |
| **B11** | **"Did you know" blurbs in the Daily Five.** Database half is already done: the `note` column ships, the emitter carries it, `audit.py` guards it. | Only the display is left, and it is scoped to `daily.js` — ruled 08-05, blurbs live in the Daily Five and mini-games, never in the race to 11. Cheap, and the notes get written for free during Track A. |
| **B12** | **On-court name tags · fix the 27 lazy questions · CPU-vs-CPU headless sanity test · scoreboard tap-to-zoom.** | Polish and the safety net. The CPU-vs-CPU test is the one that catches what twenty humans would otherwise find for you. |
| **B14** | **THE GYM AS A ROOM.** *(Aaron, 08-09: "I do want to try and get the Gym room ready for the 20 if it's possible! It'll be the last thing we do before release.")* One room, one image, seven hotspots pointing at drills that already work. Full spec under NEXT WEEK IS THE COACH above. | **Below B13 on purpose, because Aaron put it there: it is the LAST thing before release.** It is out of order on merit and in order on instruction, and the instruction is right, because nothing gets built on top of it and so cutting it costs nothing. Blocked on one sourced gym interior. |
| **B13** | **Player skills · TV / couch mode · in-game chat · trash talk.** Chat ships WITH mute, canned phrases and a report path — not as follow-ups. **THE SHAPE IS RULED, 08-17:** *"for same phone local or online play we just have prefixed emojis and prefixed things to say so we can avoid troubling chats and that's what we will release as trash talk for now."* So v1 is a CLOSED SET, canned lines plus emoji, no free text at all, which removes the moderation and report-path cost that put this last. The CPU gets its own bank (*"what's the holdup!?"*, *"you scared!?"*) and the whole thing has an off switch. The CPU half rides with **B17** because it is part of making the turn feel answered; the player-to-player half stays here. | The four Aaron pulled forward and kept after being shown the cost. **That is his call and it is not being re-litigated** — they are last because they are the biggest, not because they are optional. |
| **B18** | **THE SMOOTHNESS PASS.** Aaron, 08-17: *"there is something mechanical lacking smoothness about how the whole game plays, like when you click a button to do something the transition is too fast almost glitchy like, even when you click quick run or jacket and the soon slams down, it's so quick it feels uncomfortable... the game is lacking an overall quality smoothness I think, and that's across EVERYTHING in the game... even the sounds are these short fast bursts that feel unnatural compared to smooth gaming."* Not a screen, a SYSTEM: screen transitions, button presses, the SOON slam, the sounds' attack and tail. Scope owed before any build: measure every transition duration and easing in the product, find the ones under the threshold where a change reads as a jump-cut rather than a move, and propose one shared motion vocabulary rather than 40 hand-tuned numbers. **Diagnosis first, and it must be a MEASUREMENT, not an opinion.** | This is the only item on either track that a player would feel on every single tap. It also cuts across everything, so it wants doing before more surfaces are added on top of it, not after. |
| **B19** | **MENUS THAT LEAD YOU.** Aaron, 08-17, from the Apple Store app at checkout: *"when picking things in a menu there is this process that happens where you see the down arrow but it's unclear you have anything to do... what you need to do is highlighted and then once you do it or select your thing it pulls you down to the next thing, and then when you are done a transparentish complete menu comes up showing all you have selected and to go on to the next thing, and you can even collapse that little summary."* Four mechanics in that: the ACTIVE step is visibly the active step, choosing SCROLLS you to the next one, a COLLAPSIBLE SUMMARY of your picks appears at the end, and the continue action lives on the summary. Screenshot filed with the message. **He offered a screen recording, and it should be taken:** the timing between the steps is the part a still cannot show. | Setup is the first thing twenty people touch and the last thing anybody has looked at. It also feeds **B9**, because a setup that leads you is most of what makes a Quick Run screen work. |
| **B20** | **THE TOSS-UP BUZZERS.** Aaron, 08-17: *"why is the buzzer for the player on top, it should be on the bottom. Or make them look like real buzzers and put them left and right. Can you do either or compare them?"* A comparison is owed, not a pick: bottom-anchored as-is, versus two real buzzers left and right. Note the second is not just a move, it is a different object, and left/right also answers the same-phone case where two thumbs need their own halves. | Cheap, visible, and it is on the very first screen of a head-to-head game. His own read is that the current one is backwards, and he is right that the player's control belongs under their thumb. |

| **B15** | **THE PACK REVEAL BUILD · BACKLOGGED 08-12, superseding the 08-11 GO. Aaron: "I think I may pull back on the packs idea for now, like the collectibles and stuff... so let's really backlog that for a while, and get the game for the 20 nailed down first."** The legal grey zone around names-as-collectibles (LEGAL.md Finding 5) is the context; the packs question stays in the counsel memo. Everything below stays filed as the approved spec for whenever it wakes: ruled 08-11 ("The rarity mock is good" · "Reveal sounds are good to ship"). Port the approved mock into the real reveal: value-sum rolls (Role 1 · All-Star 3 · Superstar 5, bands Common 7-9 through HoF 22-25, floor rule one All-Star minimum, roll odds unchanged 40/28/20/9/3), the flip-tick-fill-count staging, the pack type SLAMMING beneath the meter, the V2 synth sounds (sourced-audio v2 is post-launch, D39), quake on Epic+, HoF golden choir, tap-to-skip. Spec IS the approved mock: <https://claude.ai/code/artifact/4994680a-bf07-4ead-9d39-f8b5199517aa>. Harness + comparison artifact before merge. **Queued directly behind B5c, which was ruled first.** | Closes B5's sixth defect (rarities invisible) with Aaron's own staging pick, and it is the game's biggest earned-reward moment: the reveal is what makes a pack feel like anything at all. |
**Backlogged from Aaron's desk, his word 08-11 ("Throw in backlog"):** branch
protection for `main` + deleting the three stale branches. Both approved, both
only he can click, neither urgent; resurface at launch prep.

---

### What is deliberately NOT on either track

Recorded so it cannot creep back in as "surely this is launch work":

- **Everything outside NBA and WNBA.** College, BIG3, streetball, overseas,
  Flags, Early Black Basketball — they stay in the tables and out of the packs.
- **The Black Fives letter and the H3 run.** V0's own "NOT in V0" list. Aaron's
  call, recorded as his call.
- **The completeness census (V28), the Tape's third tab (V30), the crossover duel
  ratings (V31).** Real work, wrong side of the launch.
- **The Daily Five leaderboard** — *"Leaderboard is long term vision."*
- **Streak rewards** — all four accepted, all four post-launch.
- **The scoreboard/play-by-play redesign, and hype sheet v2.** *(Drills-as-tutorial WAS on this list and is not any more: Aaron moved it into Track B as B7 on 08-07.)*

### The next move on each track, right now

- **Track A:** ~~V29 Run B~~ **done**. Aaron rules on V36 (the slang cards) and
  decides whether to send the Sports Reference letter. Claude can start A3 or
  A3b without either.
- **Track B:** ~~the Daily Five~~ ~~add to home screen~~ ~~the install prompt~~
  ~~B5b, the three cheap Daily Five defects~~ ~~the main menu redesign~~
  ~~the em dash sweep~~ ~~B3, the invite link~~ ~~B4, the wake lock~~
  **NEXT WEEK IS THE COACH**, blocked out as one job by Aaron on 08-08: B7, D7,
  the Gym as a room, B5's drill defects and the `bk_install_seen` question. They
  all touch `coach.js` and they share one unanswered question, how much the
  Coach is allowed to say before he is annoying.
  *(**RULED 2026-08-11, Aaron: "Yes to the tours model." The coach block is
  open.** There is no budget NUMBER: the cap is structural. Game one is the
  hello, three tours (14 steps, each skippable as a block, about 90 seconds
  combined), the first-card mini-tour, then only triggers as things happen, at
  most one per possession. The full model and all 256 coach moments are filed
  in `design/COACH-TOURS-2026-08-10.md`, coverage proven by
  `tools/coachtours-count.py`; the drill layout half was already RULED in
  `design/COACH-BOARD-2026-08-10.md`. `tools/next.py` no longer holds B7 and
  B14.
  **He added one thing to it and it is already BUILT:** reaching for the exit
  raises "Skip remaining tips?" over "You can reference the rulebook in the
  pause menu or turn coach back on." Live on the tip's Coach off button today,
  which used to kill the Coach in one silent tap; `BKCoach.askSkip()` is the
  shared component so B7's tour skip asks the same question. 27 checks in
  `tools/skip-confirm-check.mjs`, including that both doors the copy promises
  really exist in the pause menu.)*
  ~~B6, the in-game feedback button~~ **DONE 08-09**, built in the window while
  Aaron was blocked on art and the coach rulings. After that, B5c (the Daily
  Five theatre), which is no longer blocked on sound: the crowd cheer arrived
  on 08-09 and only needs the files moved into the repo.
  *(The old text here: **B3, the invite link and retiring the access code.**
  Named in this file as the biggest drop-off risk at launch. Shipped, and the
  gate stayed ON with the pass riding in the link, which was Aaron's call.)*

---

## The build — 23 items

**Data & trust:** verified-pack gate (build the TEST before the research, so
"done" has a definition) · packs serve verified cards only · re-link every card
to its fact

**Fun — the answer to "I've never finished a game to 11":** fouls and free throws ·
~~heat multiplier~~ **heat ABILITIES** + the bar + ON FIRE (needs the popup, the
sound, and a persistent readout) · desperation heave · battles resolve on a
tiebreak stat, **with the numbers popping on screen and the winner glowing** —
Aaron's addition, and the tiebreak does not ship without it

> ⚠️ **Corrected 08-03.** This file said "heat multiplier". It was locked 08-01;
> on **08-02** Aaron locked the opposite in `DESIGN.md` §6 on the 22af Run A
> evidence — *"Heat pays out in ABILITIES, never in point multipliers"* (NBA Jam's
> shape, verified from the official manual; Balatro shows why multipliers break a
> race to a fixed target). DESIGN.md owns the rules, V0.md owns the plan, so this
> is stale shorthand corrected, not a decision reversed.

**Twenty people who owe you nothing:** Quick Run · The Daily Five · cards remember
you between games · play logging · retire the online access code · wake lock ·
in-game feedback button · add to home screen · handle the sleeping server
gracefully · an invite link that just works

**Polish that is not optional:** shot effects (arc trail, swish burst, rim rattle) ·
on-court name tags · CPU-vs-CPU headless sanity test · give sudden death a home ·
fix the 27 lazy questions

### THE 27 ITEMS, CHECKED AGAINST THE CODE — 2026-08-06

Aaron, 08-06: *"I am not launching to the 20 until I get to where I said I want
to be for them. That includes the home screen app and so much more. I feel like
you did not scan the status artifact correctly."* He was right. The list above is
prose and nothing has ever tracked it item by item, so "are we close to launch"
has been answered from feel. Grepped against `docs/play/` — **10 done, 1 part-done,
16 not started.**

**DONE (10)**
- [x] verified-pack gate · [x] packs serve verified cards only *(both 08-06)*
- [x] re-link every card to its fact *(08-04)*
- [x] fouls and free throws · [x] desperation heave · [x] tiebreak battle
- [x] shot effects · [x] sudden death has a home
- [x] The Daily Five *(08-06)* · [x] The Tape rebuild

**PART-DONE (1)**
- [ ] heat ABILITIES + bar + ON FIRE — the bar, the abilities and the popup ship.
  **The sound does not exist.** V0 asked for popup + sound + persistent readout.

**NOT STARTED (16)** — and this is the real distance to launch
- [ ] Quick Run *(the phrase appears only in rulebook prose, not as a mode)*
- [ ] cards remember you between games
- [ ] play logging
- [ ] retire the online access code *(still live by CHOICE: the invite link
  carries the code, so the alpha works gated. Clearing `BK_ACCESS` in Render
  is Aaron's one-field lever if he ever wants it fully open — see the
  "OTHER HALF OF B3" item above. Not owed work.)*
- [x] ~~wake lock~~ **DONE 08-08 (B4)** *(2 `wakeLock` refs in game.js,
  re-acquired on visibilitychange · mark flipped 08-11, the B4 commit
  should have flipped it)*
- [x] ~~in-game feedback button~~ **DONE 08-09 (B6)** *(feedback.js, three
  entry points, 27 checks · mark flipped 08-11)*
- [x] ~~**add to home screen — there is NO manifest file at all.**~~ **DONE
  2026-08-07.** `docs/play/manifest.webmanifest` + the iOS meta tags (Safari
  ignores the manifest and reads `apple-mobile-web-app-*`, so both are needed
  and neither is redundant) + `icon-maskable-512.png`, generated by
  `tools/make-maskable.py` because the existing icon draws its ring almost
  edge to edge and every round launcher mask would have sliced it.
  Guarded by **`node tools/pwa-check.mjs`, 24 checks**, which fetches every
  icon rather than trusting the path and drives the shortcut url for real.
  **STILL OPEN, and both are filed below:** no service worker, so Chrome
  offers "add to home screen" but not a full install; and `short_name` is
  "Ball Knowledge", which iOS truncates on the home screen.
- [x] ~~handle the sleeping server gracefully~~ **DONE 08-08 (B4)**
  *(game.js "Phase 1.5: the server naps, wake it FOR the player" ·
  mark flipped 08-11)*
- [x] ~~an invite link that just works~~ **DONE 08-08 (B3)** *(`inviteUrl()` +
  `?join=CODE&k=` deep link, 17 checks in invite-check.mjs · mark flipped
  08-11)*
- [ ] on-court name tags *(`courtName` is the COURT's name, not a player tag)*
- [ ] CPU-vs-CPU headless sanity test
- [ ] fix the 27 lazy questions
- [ ] TV / couch mode · [ ] player skills · [ ] in-game chat *(+ mute, canned
  phrases, report path — they ship WITH it) · [ ] trash talk

**The honest read:** the data track finished today. The BUILD track is roughly
40% done, and the ten-item "Twenty people who owe you nothing" group — the one
that exists purely to make twenty strangers play twice — is **1 of 10**.

## Pulled forward into V0 by Aaron — all five stay

TV / couch mode · player skills · in-game chat · The Tape rebuild ✅ · trash talk

He was shown the scope cost and kept all five. **That is his call, recorded as his
call. Do not re-litigate it.**

⚠️ **Chat and trash talk carry a condition.** Aaron parked in-game chat on
2026-07-25 and flagged the moderation risk himself. Moving it up does not delete
the risk: **mute, canned phrases where they will do, and a report path ship WITH
it**, not as follow-ups.

## NOT in V0 — Aaron's call

spacing playtest verdict (Open floor already ships as the default; the verdict is
feedback, not a build) · drills-as-tutorial · Black Fives label + Foundation
outreach · scoreboard/play-by-play redesign (still blocked on Aaron's reference
art) · hype sheet v2 · test-kitchen verdicts each round

**Daily Five leaderboard — LONG TERM, Aaron 08-04: *"Leaderboard is long term
vision."*** Recorded here so it is not lost and not mistaken for V0 work. Worth
knowing before it starts: everyone getting the same ten is what makes a
leaderboard possible at all, and the local `bk_daily5h` history is per-device —
a real leaderboard needs the server, an identity, and a way to stop people
editing their own score. Comparing in the group chat is the V0 answer, which is
what the share receipt is for.

**Streak rewards — ALL FOUR ACCEPTED 08-04. Aaron: *"I love all 4 rewards and we
will build them later."*** Not V0; build order below is mine to propose when the
time comes. Costed honestly at the time of proposing.

> ⚠️ **Read this before choosing any of them.** The streak lives in
> `localStorage` on ONE device. Clearing the browser or changing phones erases a
> 30-day run. A reward big enough to be worth chasing makes that loss much worse
> than it is today, so anything past the cheap option really wants the server and
> an identity first — the same dependency the leaderboard has.

- [ ] **A · Raise a banner to the rafters — RECOMMENDED.** Hit 30 and the arena
  rafters drop in, a championship banner unfurls and rises into place: the month,
  the crown/star count, the year. **It stays up forever**, and month two hangs
  next to month one, so the reward compounds instead of evaporating. Most
  basketball-native thing available, and the only option that gets better the
  longer someone plays.
  *Medium: BUILD IT.* A banner is flat fabric, type, a rope and a slow rise —
  vector and CSS geometry, which is the side of the line where hand-coding is
  genuinely good. A painterly arena interior BEHIND it would be sourced art, and
  it does not need one; the existing court backdrop already works.
  *Cost: ~1 day for the banner and the moment, ~1 more for the rafter room that
  accumulates. Share needs canvas→PNG, roughly half a day.*
- [ ] **B · The month card.** A trading card minted for the month in the game's
  own card language — best day, crowns, longest streak, a grade. The most
  shareable of the four, because an image beats text in a group chat.
  *Medium: BUILD IT.* Cheapest if it reuses the pack card styling.
  *Cost: ~half a day plus canvas export. Weakness: it is an artefact, not a
  moment. Best paired with A rather than instead of it.*
- [ ] **C · A championship ring in a trophy case.** The most obvious basketball
  reward and the most dangerous to build.
  *Medium: SOURCE THE ART.* A ring is metal, stones, depth and reflection — a
  rendered or illustrated object. Hand-coded it lands at a gold donut and it
  would be over-promising to pretend otherwise. If Aaron wants this, the ask is
  a single 3/4-view ring, gold with a dark stone, transparent background, plus
  one "empty slot" state for the case.
  *Cost: low to build the case; the art IS the feature and it needs commissioning.*
- [ ] **D · The stamp itself levels up — CHEAPEST.** The menu calendar page takes
  a gold foil edge at 7 days, a flame edge at 30, something else at 100. No
  popup, no share, but it is permanent, seen on every single launch, and it
  reuses the fire art already in the repo.
  *Cost: hours. Weakness: it is not a moment and it does not leave the game.*

**All four are in. Suggested order when they come up:** D first (hours, and it is
seen every launch), then A (the moment), then B (the thing that leaves the game),
then C — because C is the only one gated on commissioning art, and it should not
hold up the other three.
**Do not start any of them before the streak survives a device change.** A
30-day run erased by clearing a browser is a bad feeling today and a much worse
one once there is a banner hanging on it.

---

## Build order — the RECORD of how we got here, not the plan

> **Superseded 2026-08-07 by THE ORDER, TWO TRACKS above.** This numbered
> sequence is kept because its entries carry measured status detail that
> exists nowhere else (R1 pass one, the Daily Five scope bug, what is and is
> not built in the heat block). **Read it for state, never for order.**


1. ✅ **R0** — the todo table
2. ✅ **The Tape rebuild** — Aaron: *"nothing matters if I can't navigate and look
   at the data correctly"*
3. ✅ **22af research run** — filed at **`design/22af-findings.md`** (517 lines,
   28 sections, committed `bc3c7bb`). Runs **one, A, B and C** are done and Aaron's
   rulings are logged against each. **Runs D and E were never run** — D is the
   player wishlist; E is lanes 3 and 5, and no public "no one else has built this"
   claim is allowed until E lands, because Run C already refuted the moat as
   originally worded.
4. ✅ **The verified-pack gate** — BUILT and PROVEN, and deliberately **shipping
   DARK**. `PACKGATE={verifiedOnly:false}` in `game.js`; `tools/build-verified-index.py`
   generates `unverified-index.js`; `tools/verified-gate-check.mjs` is 9 checks
   with a break-proof. **Flipping it today would empty the NBA and WNBA pools** —
   835 of 1,526 cards are R1 source-dead — so the flip is gated on R1, exactly as
   intended: the test exists before the research.
5. 🟡 **R1–R8** — R1 STARTED 08-03, R3 ESSENTIALLY DONE, R2/R5–R8 untouched.
   **R3: 513 → 3.** Source tier built (`tools/tier-sources.py`), tiers derived
   from the publisher map in `DEEPRESEARCH_KNOWLEDGE.md`, fact confidence
   calculated from them. Spec in `TABLES.md`.
   **R1 pass one: 731 label-only sources given the url they always had.** They
   were never missing — the ids came from the original research runs and those
   files still carry the urls; the paperwork was done and never filed. Facts
   that can ship went **151 → 258**. Source rows pointing nowhere: 1,107 → 376.
   ⚠️ **R1's own counter has NOT moved (still 829)** and that is honest: it asks
   whether a card points at its FACT id, and cards still carry a url instead.
   Making cards point at fact ids is a further step that trips `audit.py`'s
   `srcids_unresolved` ratchet, so it needs Aaron's call first.
   ⚠️ **What was recovered is not automatically RIGHT.** 529 of the 731 point at
   Wikipedia — Tier 3, never ships alone, so the gate contains them. But at
   least one is plainly mis-mapped: the Red Auerbach nine-titles card came back
   citing a Britannica page about *Phil Jackson*. Recovery restores what was
   written down; it does not check that it was correct.
6. 🟡 **Fouls & free throws → heat + bar + ON FIRE → tiebreak battles → the heave**
   — **only the heat third is built.** ✅ heat: the bar, the pour/drop rules, both
   abilities, extinguish, the ON FIRE slam, the fill-bar readout, and all the
   sourced flame art (pillar, ember rings, ball burning in hand and in flight);
   `tools/heat-check.mjs`, 20 checks, break-proofed.
   ❌ **heat has NO SOUND** — this file asked for "the popup, the sound, and a
   persistent readout"; popup and readout shipped, sound never did. `heatCard()`
   plays nothing and there is no fire/heat cue in `audio.js`.
   ❌ fouls & free throws · ❌ tiebreak battles + the numbers popping · ❌ the
   desperation heave (the only "heave" in the code is the full-court PASS label).
   DESIGN §6 phase 2 also still unbuilt: streak mode, heat-check bomb, posterize
   drain, pass/dunk windows.
7. ⬜ **Player skills** — not started.
8. ⬜ **The "owe you nothing" block** — none of it: no Quick Run, no
   cards-remember-you, no wake lock, no sleeping-server handling, no feedback
   button, no manifest/add-to-home-screen, no invite link. The online access code
   is still in place and still needs retiring.
9. ⬜ **Play logging + CPU-vs-CPU sanity test** — not started.
10. ✅ **The Daily Five** — **BUILT, 48 checks green, AND MERGED.** Verified
    2026-08-07 with `git cat-file -e origin/main:docs/play/daily.js`: it is on
    main and byte-identical to the branch. This entry read "NOT MERGED" for a
    day after it went in, and a later plan was written on top of that
    sentence — a stale status does not sit still, it gets built on. Two rounds of five, date-seeded so
    everyone gets the same ten, plus the Heat Check bonus round on a sweep.
    **It was built out of order** — item 10 ahead of items 5–9 — because it was
    asked for directly. Owed before it merges: the before/after artifact and a
    desktop pass.
    **SCOPE BUG FOUND BY AARON PLAYING IT, fixed 08-03.** The picker bypassed
    every filter to guarantee one shared set, and bypassed the V0 league
    boundary with them: measured over 30 real days, **106 of 300 cards (35%)
    came from Flags, college, BIG3, Black Fives, streetball and overseas, and
    all 30 days served at least one** (worst day 8 of 10). Aaron: *"it's not fun
    to be asked about something you have no understanding of."* Fixed by
    honouring the boundary, NOT by adding a per-player league setting — a
    setting would split twenty testers into twenty different games and kill the
    "did you get today's?" that is the only thing a daily has. Everyone still
    gets the identical ten. Cost measured at zero: thinnest slot is 156 cards.
    The Heat Check answer pool took the same boundary, 86 → 65 candidates.
11. ⬜ **TV / couch mode** — not started.
12. ⬜ **Chat + trash talk**, shot effects, on-court name tags, sudden death's
    home, the 27 lazy questions — none started. Sudden death itself EXISTS as a
    mechanic (`game.js`); the V0 item is giving it a home in the UI.

## Still open on things already built — filed 2026-08-04

Raised while building, and every one of them was a sentence in a chat reply
before it was a line in this file. `python3 tools/open-items.py` lists these
alongside every other open item in the repo.

- [ ] **"Did you know" blurbs on the cards worth one.** Aaron, 2026-08-05,
  after being told why the free-throw line is measured to the backboard:
  *"I wonder if we can put little fun learnings on question cards that have
  something like this, where if you get it right a small blurb might pop up
  telling the little story... it's a fun occasional thing for interesting facts
  and this is all about learning and information after all."*
  Type C — build, no research. **The database half is DONE** (08-05): `facts`
  has a `note` column, the emitter carries it onto the card as `note:`, it is
  documented in TABLES.md, and `audit.py`'s `notes_unsourced` fails the gate if
  a note appears on a card nobody has verified. One note is written — f-0104,
  the backboard story that started this.
  **WHERE IT LIVES — RULED 08-05. Aaron: *"blurb should live in daily 5 and any
  other mini games we develop along the way."*** So: the Daily Five and future
  mini-games, and **NOT the live game to 11**, which is a race and would turn
  into a lecture. That also means the display work is scoped to `daily.js`, not
  `game.js` — a much smaller job than it looked yesterday.
  **Still open:** whether a MISS gets one too. See the item below, because that
  question stopped being what it was.
  Medium check: text, a panel and a transition — **vector/CSS, build it.**
  Nothing here needs sourced art.
  **Filling the field is free right now and expensive later.** Every
  verification pass has the source page already open; writing the blurb at that
  moment costs a minute, writing it afterwards means re-reading everything.
  That is why the column shipped before the popup.

- [x] ~~**The main menu's scroll chevron pointed at nothing.**~~ ✅ **FIXED
  08-05.** Aaron, from a desktop screenshot: *"there is nothing to scroll down
  on, so technically it shouldn't be there"* — and it was sitting on top of the
  *est. 2026* line. The chevron's logic was never wrong: the title screen
  really did carry **233px of scrollable overflow at 1440×900**, and every
  pixel of it was `.slash.s2`, a decorative diagonal whose ROTATED box runs
  251px past the last real content. Transforms count toward a scroll
  container's overflow. The slashes now sit in a clipping layer; nothing
  visible moved, because the clipped part was below the fold already.
  Guarded in `smoke-check.mjs` by SHAPE, not by number — nothing may sit below
  the last real element on the title screen — so the next decoration that
  overflows fails the harness instead of shipping a chevron that lies.

- [x] ~~**The Daily Five stamp only looked clickable for 0.4s at a time.**~~
  ✅ **FIXED 08-05.** Aaron: *"someone may not be on the main screen long
  enough the first time to notice… it can just look like a date otherwise."*
  Measured: the old pulse ran 3.4s and lit the ring only between its 88% and
  94% keyframes, so **five sixths of the time the stamp had no affordance at
  all.** The ring is now the RESTING state while today is unplayed, and the
  pulse breathes continuously instead of blinking. It reads as a control in a
  still screenshot, with animation off, and under reduce-motion. Hover was
  already good and is untouched.
  **Still open if he wants it louder:** the stamp leads with a big date, and
  the honest next lever is an explicit *TAP TO PLAY* chip or a smaller
  numeral. That is a call about what the object IS, so it was not made
  unilaterally.

### Playthrough findings — Aaron, 2026-08-05

Nine items from one desktop session, filed the turn they were raised. Two of
them are almost certainly ONE bug; one of them is a feature he has wanted for
a while and finally described properly.

- [x] ~~**The Daily Five label should be a call to action.**~~ ✅ **DONE 08-05.**
  Now a small mono kicker *RUN YOUR* above the display-face *DAILY 5* — two
  lines, because at 180px (126px at 390) one long string would have shrunk the
  name to a size the card is built around. Measured to fit at both widths with
  no wrap. **The played state hides the kicker**, because the first version
  read *RUN YOUR DAILY 5 / BACK TOMORROW* — an instruction directly above a
  note that you cannot. Both halves guarded in `smoke-check.mjs` and both
  sabotage-proven.
  ~~Aaron:~~ Aaron: *"let's change
  the 'Daily 5' to maybe 'Run your Daily 5' or something creative, so it's clear
  to click and run it or play it."* Follows straight on from the resting-ring
  fix — the ring says *this is a control*, the label should say *what pressing
  it does*. Watch the width: the stamp is 180px desktop / 126px at 390, and the
  label sits at 22px display type, so a longer string needs a size or wrap rule
  rather than just new words.

- [x] ~~**LEAVING A DRILL DOES NOT TEAR THE DRILL DOWN.**~~ ✅ **FIXED 08-05.**
  Reproduced first, then fixed: leave a drill by any route other than its own
  ✕ End drill and the state came back `coach:true, drillOn:true`. The cleanup
  lived inside `endDrill()`, which also navigates — so the one exit that
  navigated was clean and every other exit was not. `drillTeardown()` is now
  its own function and `show()` calls it on any move off the game screen,
  ABOVE `musicSync()` so the track resolves after `DRILL.on` clears.
  `tools/drill-check.mjs` walks four exits and asserts the coach card, the
  DRILL banner and the music resolver on each. Broken on purpose: removing the
  one line in `show()` fails 9 checks; restoring it passes 16.
  Two harness faults found while proving it, both of which would have shipped
  a green tick over an untested fix — see AI-LEARNINGS 1.2r.
  ~~Two reports, one cause,~~
  and Aaron's screenshot of the main menu shows both at once: the coach card is
  still on screen — *"Now tap a teammate — choose Pass"*, with Restart and End
  drill buttons — and the boombox still reads IRONY, the drills track.
  `endDrill()` in `coach.js` clears state and calls `coachHide()`, so the exit
  through the drill's own button is fine. What is NOT covered is leaving by any
  OTHER route — the menu, the back control, whatever Aaron used. `musicWant()`
  returns `'tutorial'` while `DRILL.on` is true, which is exactly the symptom.
  Fix at the teardown, not at the two symptoms: one place that ends a drill,
  called by every exit. Then assert it — go into a drill, leave by each route,
  and check the coach is gone and the track is `menu`.

- [x] ~~**The coach card covers the board during drills.**~~ ✅ **FIXED 08-11,
  and the obvious reading of it was wrong.** Before/after at both sizes, plus
  the drill audit and the three new drills:
  <https://claude.ai/code/artifact/199cd2cf-4a74-4cc2-b987-a5ce6bb0bb65>
  "Covers an action" sounded like
  tiles, so the first cut hunted tiles. Measured, no lit tile lands under the
  card on a 390 phone in ANY drill sandbox. What the card really covers is the
  **stagebox** · the row holding `CONFIRM ✓` / `Pass ✓` · by **14px at
  1440×760 and 1px at 390×844**, and the desktop screenshot shows it sitting
  clean across both buttons while the card says "hit Confirm ✓". Two fixes,
  because there were two faults: `pointer-events:none` on the shell (the card
  was eating taps meant for the court) and `panelDodge()` in `coach.js`, which
  flips the card above the HUD whenever the stagebox or a lit tile falls under
  it. The flip lands at the measured HUD bottom, not a fixed offset · 60px was
  clean at 390 and sat across ORANGE / BLUE at 1440. Proven by
  `tools/drill-b5-check.mjs` at both sizes, and the sabotage in it asserts the
  14px overlap is really there, which is how the first version of the check
  was caught passing at a viewport where nothing could ever overlap.
  ~~Old note:~~ Aaron: *"sometimes the
  coach covers an action, like a pass, when in the passing drill, and selected
  another player."* Worse than clutter — it is eating taps meant for the game.
  His screenshots show it parked over the lower court. Wants a position that
  cannot overlap live tiles, or a card that moves out of the way of the tile it
  is talking about.

- [x] ~~**D38 · REAL COURT LINES.**~~ ✅ **DONE, and ACCEPTED 08-11: "Court
  is good," after his third read fixed the FT-circle halves and snapped the
  corner rail to the grid. Closed for real this time.** His catch on the first exact
  draw was correct and named the root: *"how is the three point line
  interesting the foul shooting area? This is very incorrect."* The arc cut
  the free-throw circle because the RIM sat 1.9ft outside the baseline (day
  one geometry) where a real rim is 5.25ft inside; measured, that one error
  eats 7.5ft of arc clearance. Fixed at the root rather than the picture:
  rims moved INSIDE to the real 5.25ft, glass to 4ft (true 1.25ft bracket
  gap), all rule distances re-expressed in feet (`FTX/FTY` per mode, half
  court 47ft), and **the drawn 23'9" line IS the scoring rule now**
  (`ARC_FT=23.75`, corner strip computed from the same arc). No new squares
  needed: 15x8 measures 0.3% off true proportions and spaces exactly once
  the rim is inside. Tier tints stay per his ruling. Measured shipped map
  per side: 26 tiles worth 3 (22 arc + 4 corner) · 16 mid · 10 layup. Full
  harness suite green; geometry locked in DESIGN.md § 1. Comparison:
  <https://claude.ai/code/artifact/e65ddf15-64b2-4476-89ad-48fac5a75913>
  ~~First exact cut, superseded same day by his catch:~~ Aaron sent the diagrams: *"use these photos and try and
  get it as close to exact as possible with good spacing"*, pre-authorising the
  grid surgery: *"I don't care how much you need to redo, even if you have to
  make the court itself larger."* Built same day at true NBA numbers scaled
  onto the floor (grid measures 1.2% off real 94x50 proportions): 16x19ft key
  with lane hash marks, 6ft FT circle solid/dashed, 4ft restricted arc, 23'9"
  arc meeting rails 3ft off each sideline (the 22ft corner exactly), 6ft/2ft
  centre pair. **Plus his same-day ruling: "yes tint the tiles to match
  difficulty, that should stay" · built, the staircase borders replaced by
  faint tier fills under the lines, same TIERS colours as the cards.** Still
  behind `LINES.real` until his read. **The two open
  choices, both measured:** (1) retune SCORING to the drawn arc · moves 20
  tiles-worth-3 to 25 per rim, a balance shift to choose, not slip in; (2)
  **phase 2, the court enlargement**: the rim sits 2.2ft outside the baseline
  where a real one is 5.25ft inside; fixing it means adding a column each end
  (15x7 grid), which touches movement ranges, zones, drills, CPU and net ·
  pre-authorised, filed as its own build block.
  ~~First cut, superseded same day:~~ built behind a flag, awaiting read.
  Aaron, 08-11: *"the lines on the game court are a bit confusing... make them
  like the ones on a real court but leave the squares as they are... My friend
  played and was very confused, especially because the outermost line isn't
  even a line on a court."* First real-tester confusion report, which makes it
  high signal. Built same day: `LINES.real` in game.js draws baseline, key +
  free-throw circle, restricted arc, and a true three-point line (corner rails
  meeting the arc), every measurement referenced from the LIVE rule constants
  (185 arc, `inPaint`, the corner strip) so the floor cannot drift from the
  scoring. The tier staircase lines are gone in this mode; difficulty still
  shows on lit tiles and cards. Live default is unchanged until he rules.
  Comparison, both court types:
  <https://claude.ai/code/artifact/e65ddf15-64b2-4476-89ad-48fac5a75913>
  One found bug already fixed in the build: the mirrored rim's arc swept the
  short way round and drew behind the baseline (angle wrap at PI).

- [x] ~~**D39 · THE PACK-REVEAL SLAM SOUNDS.**~~ **RULED 08-11: "Reveal
  sounds are good to ship but we will do a v2 on those after we launch to the
  20." So the V2 synth set SHIPS with the reveal build, and a sourced-audio
  upgrade is POST-LAUNCH work, not owed for the twenty.** Sourcing list and
  per-rung escalation spec stay filed here for that day.
  ~~Old text:~~ synth V1 rejected, V2 delivered, his ear decides. Aaron, 08-11: *"those sounds for
  the pack reveals aren't going to do it, very anticlimactic need some ideas
  for sourced sounds if you can give me some orrrr if you can make something
  different."* V2 rebuilt in the mock (hall tails via feedback delays, braams,
  sub drops, the golden choir on HoF). Honest ceiling stated: synthesis gets
  close but sourced audio goes past it, and the game already has the sourcing
  precedent (Ketsa music, the arena art). Spec if he sources: five files,
  `docs/play/audio/pack-{common,rare,epic,legendary,hof}.mp3`, each under
  ~100KB, escalation shape per rung filed in the mock's own page. Self-hosted
  per house rules, license noted in PLACES.md like the music was.

- [ ] **The flipped coach card covers the `DRILL · MOVING THE ROCK` banner.**
  Noticed while shooting the B5 before/after on 08-11, not fixed, so it is here
  rather than in a chat reply. When `panelDodge()` flips the card up it lands at
  the measured bottom of the HUD, which is exactly where the drill-name strip
  lives, so the strip is hidden for as long as an action is staged. Costs
  little · the card itself says what the drill is teaching · and the alternative
  is pushing the card down over the court, which is the bug it just fixed.
  Cheap options if it bothers Aaron: shrink the strip, or move the drill name
  into the card. **His taste, small either way.**

- [x] ~~**Drills should refuse the moves that are not part of the drill.**~~
  ✅ **FIXED 08-11, both of his options rather than one.** The refusing half
  already existed; the missing VISUAL half is now `drillGrey()` in `coach.js`,
  which desaturates any action button the current drill will bounce. The tap
  still lands, on the Coach's line, because a dead control with no explanation
  reads as a broken game and during a tutorial that is the worst possible read.
  The button-to-kind mapping is published by `BK.drillKinds()` and the staged
  case reuses `stagedKind()`, the same function the gate itself calls, so the
  grey and the gate cannot drift apart. If Aaron wants the controls fully dead
  instead, that is now a one-line change rather than a build.
  ~~Old note:~~ Aaron:
  *"I want all other actions to be greyed out if they have nothing to do with
  the current drill. They can be there because that's reality for the game, but
  they shouldn't be clickable, or they can be clicked, and nothing happens, and
  the coach says something like 'whoops, wrong move'."*
  Half the machinery exists: `DRILL.allow` / `DRILL.deny` are already in
  `game.js` and `drillAllows()` is already consulted. What is missing is the
  VISUAL half — the greying — and a decision between his two options. My read:
  grey them AND let the tap land on the coach's line, because a dead control
  with no explanation reads as a broken game. That is a taste call, so it is
  his.

- [x] ~~**The boombox is still too small on desktop.**~~ ✅ **FIXED 08-05.**
  It was 237×162 at EVERY desktop width — 16% of the screen at 1440, 12% at
  1920 — which is why it kept reading small whatever the monitor. Scales now:
  1.18× past 1200px, 1.34× past 1700px. Scaled rather than widened, because
  the internals are fixed px and widening the shell alone would stretch the
  case around unchanged dials. The 390 mini tab is untouched.

- [x] ~~**Type too small on Pick Your Roster — three separate places.**~~
  ✅ **FIXED 08-05.** All three were 9px letterspaced mono. Shuffle counter and
  its dots 9→13px, *tap a card for full stats* 9→12px, the odds line 9→12px,
  and the faint ink lifted to dim because size alone does not fix contrast.
  **`smoke-check.mjs` now fails on any of those under 11px** — it had always
  measured CONTROLS under 28px and never once looked at TEXT, which is how 9px
  survived. Sabotage-proven.
  ~~Old note:~~
  *"3 SHUFFLES LEFT"* and its orange dots, *"TAP A CARD FOR FULL STATS"*, and
  the instruction line along the bottom of the screen. Measure them against the
  existing floor before choosing sizes; `smoke-check.mjs` already tracks
  controls under 28px, but these are TEXT, which nothing currently measures.

- [ ] **A player cannot see what the pack rarities are, so re-shuffling has no
  visible stake. DIRECTION RULED 2026-08-11, samples delivered, his pick
  pending.** Aaron: *"we just want them to know the rarities that are viable,
  and it be super clear where they landed on the ladder"* plus a second,
  bigger ask: *"we should assign a value to each card tier and each pack
  rarity requires a sum up to a certain value... I want you to do this and
  give me your suggestion."* Delivered:
  <https://claude.ai/code/artifact/d3fef84c-de12-4ed8-a9dc-54a49cca2ddd>
  · three ladder mocks (lived-on ladder / count-up meter / minimal strip) and
  the value system: **Role 1 · All-Star 3 · Superstar 5, bands Common 7-9 ·
  Rare 10-13 · Epic 14-17 · Legendary 18-21 · HoF 22-25**, every combination
  enumerated, floor rule "at least one All-Star", roll ODDS unchanged
  (40/28/20/9/3). **BUILD IS GO, 08-11 evening: "The rarity mock is good" ·
  the playable mock, which carries the 5-3-1 values and the bands on its
  face, is the approved spec, so the values ride the approval. Building it
  into the real reveal now: value-sum rolls with odds unchanged, the
  flip-tick-fill staging, the slam beneath, V2 sounds per D39.**
  **MOCK RULED 2026-08-11: B, the count-up meter, with his
  staging spec:** *"the number meter going up as the cards flip one at a
  time... a bar filling animation and sound. But what I want is that when the
  number hits the pack type slams beneath as the mayjor reveal with cool
  sounds for each pack type... The osunds would get more epic as the pack you
  landed got better."* So: cards flip one at a time, each flip ticks its
  points into the meter with sound, and when the total settles the PACK TYPE
  SLAMS beneath as the major reveal, five escalating reveal sounds up the
  ladder. Animated mock with synthesized sound requested and delivered;
  5-3-1 not yet confirmed, riding along.
  ~~Original finding:~~ Aaron: *"there is no indication of the types of packs there
  are, even to get epic, legendary, etc., so how does a player know the value of
  trying again?"* The rarity ladder is printed on the screen already — *Common
  40 · Rare 28 · Epic 20 · Legendary 9 · Hall of Fame 3* — but as a caption
  under the fold, not as something that makes a re-shuffle feel like a gamble
  worth taking. He explicitly does NOT want to copy Pokémon TCG here: *"it's
  hard to find the rules you want, and I don't want to imitate that."*
  **Needs his taste, not a fix.** Type C/D.

- [x] ~~**Rulebook · RUN THE DRILL floats at the end of a paragraph.**~~
  ✅ **FIXED 08-11.** `.rb-drill` was `display:inline-flex`, so the pill flowed
  after the final full stop and read as a word in the sentence. Now `flex`
  with `width:fit-content`: its own line, still hugging its label.
  ~~Old note:~~ Aaron's
  screenshots show it inline after the last sentence — *"…other team's ball.
  [RUN THE DRILL →]"* — which reads like a word in the sentence rather than a
  control. Should always be on its own line. Small, purely visual, no taste
  needed.

- [x] ~~**Rulebook · only one topic open at a time.**~~ ✅ **FIXED 08-11.**
  Plain accordion in `coach.js`: opening a topic closes every sibling first,
  and because the close runs before the open, re-tapping the open topic still
  dismisses it. It also scrolls into view, since a topic that opens below the
  fold has opened invisibly.
  ~~Old note:~~ Aaron: *"opening another
  should collapse the other, as it just overwhelms the screen."* His screenshots
  show four expanded at once and it is a wall. Plain accordion behaviour; the
  topics already toggle via `.rb-head` in `coach.js`.

- [x] ~~**Rulebook · EVERYTHING that needs a drill should have one.**~~
  ✅ **AUDITED AND FIXED 08-11.** He asked for the audit rather than two
  drills, and the audit is the answer: **16 rulebook topics, 7 had drills.**
  Three of the nine needed one and now have one · **Contests & blocks**,
  **Inbounds** and **ON FIRE**. The other six do not: Questions, Violations,
  Online, Camera, Music and Settings are read, not played, and a drill for a
  thing you cannot get wrong on the board teaches nothing.
  **ON FIRE hands you the fire at tip-off**, per his fighting-game note, and
  that is now a rule for every earned state: A DRILL FOR A THING YOU HAVE TO
  EARN HANDS YOU THE EARNED STATE AT THE START. Superstar skills are the next
  case. Seeding is also the only route here · `heatCard()` returns early
  while a drill is on, so a fire drill that did not seed could never light.
  ~~Old note:~~ Named as
  missing: **Contests & blocks** and **Inbounds**. Aaron: *"let's make sure
  EVERYTHING that needs a drill has one."* Audit the topic list against
  `DRILLS` in `coach.js` rather than adding two and calling it done.
  **And ON FIRE needs one too, with a design note worth keeping.** Aaron:
  *"I know in some fighting games there is a meter you can fill, and in the
  tutorial you will start with your meter filled so you can try your special.
  This will be the same for special skills of superstars — we have to let
  people test it."*
  That is the right shape and it generalises: **a drill for a thing you have to
  EARN should hand you the earned state at the start.** Heat is the first case;
  superstar skills are the second, and they are not built yet, so whoever
  builds them should read this first. A drill that makes you grind to the
  feature before it can teach the feature is not a drill.

- [ ] **DECIDE WHAT SURVIVES A BACK-BUTTON, ON PURPOSE.** Aaron: *"if you go
  back when picking stuff, and then start up again, everything is saved, even if
  you have the 'add question packs' open and NBA selected. I don't mind name
  fields and stuff being saved, but having drop-downs stay open, and the league
  selected so it's open, seems like the wrong move. We have to be really
  thoughtful about what gets saved in the back-and-forth situation and what
  doesn't."*
  Filed as a RULE and not just a bug, because the same question will come up on
  every screen that gets a back arrow. His line already splits it correctly:
  - **Keep** what the player TYPED or deliberately chose and would resent
    retyping — names, colours, court.
  - **Reset** what is merely UI POSTURE — a disclosure open, a section
    expanded, a scroll position, a highlighted default.
  A selected league sits on the boundary and he called it: a selection that
  forces a panel open is posture, not a choice worth preserving.
  Needs an audit of every back-navigable screen, not a one-line fix.

- [ ] **THE PATTERN TO COPY — Aaron's own "The Coldest Call", filed 08-05.**
  He shipped this in his portfolio repo and sent four screenshots of it:
  *"it kind of greys out the background, highlights what's needed and the next
  button, and moves to the area it's speaking about. I find this to be very
  cool."*
  Five parts, and they work as a set:
  1. **The world dims.** Everything not being discussed drops to near-black.
  2. **The subject is ringed** in the accent colour — the mini-map corner, the
     player sprite, the walk buttons — so the eye has exactly one target.
  3. **NEXT is highlighted too**, so the way forward is never a hunt.
  4. **The card MOVES to the thing it is describing** — top-left for the
     location panel, beside the avatar for "this is you", down by the walk
     buttons for walking, over by the notebook for the notebook.
  5. **A counter and a SKIP** on every step: *TUTORIAL 3 / 4 · WALKING*.
  **Why this is filed as a reference and not just a compliment: it collapses
  three separate items on this list into one design.**
  - *"The coach card covers the board during drills"* — solved by (4). A card
    that moves to its subject is a card that stops parking on live tiles.
  - *"Drills should refuse moves that are not part of the drill"* — solved by
    (1). The dim IS the greying, and it reads as focus rather than as a broken
    control, which was the risk in the grey-it-out option.
  - *"Tap a card for full stats" as a first-time hint* — solved by (2) and (4)
    together, and it is exactly the pulsing-arrow-at-a-corner-card he
    described.
  So the drill coach and the first-run guide should be ONE mechanism with two
  scripts, not two features. Build the mechanism against the drills first,
  where the steps already exist in `coach.js`, then point it at the main menu.

- [ ] **THE COACH AS A FIRST-RUN GUIDE — the feature he actually wants.**
  Aaron: *"the coach shows up on a player's first time on the main menu, saying
  'first time here? I'll walk you through setup! :)' and the coach pops up and
  hovers with arrows pointing at certain areas telling them what to do and to
  make sure they do them, like add more packs, etc., and what the benefits of
  those actions are."*
  This is the home for the *"tap a card for full stats"* hint too — he
  suggested it as a first-time popup with a pulsing arrow pointing diagonally
  at a corner card. **But he was explicit that it does not replace the font
  fix:** *"This doesn't change that the font was too small."* File both, fix
  the font regardless.
  Prior art in the repo: `coach.js` already has a coach card, a step model and
  drill scripting, so this is an extension of something real rather than a new
  system. It is also the biggest item in this list by a distance and should not
  be started inside a bug-fix pass.

- [ ] **Tap the scoreboard to pause and blow it up.** Aaron, 2026-08-04, from a
  playthrough: *"during gameplay on a phone the scoreboard is pretty small, I
  mean I get it, but maybe if a player clicks or presses the scoreboard it pauses
  the game and the scoreboard drops down and you see the bigger square one."*
  Type C — build, no research, but it is a real feature and not a tweak.
  **Why it is a good idea and not just a bigger font:** the HUD scoreboard is
  small because it competes with the court for width, and at 390px the court is
  the thing that has to win. Making it permanently bigger costs the board. A
  *tap-to-expand* takes the constraint away instead of trading against it —
  nobody needs the score DURING a decision, they need it BETWEEN decisions.
  Pausing while it is open is what makes that true rather than merely convenient.
  **What already exists to build on:** `freezeGame()` / `thawGame()` are already
  in `game.js` and already used by the pause flow, and there is already a bigger
  square scoreboard drawn for the end-of-quarter and final screens — so this is
  mostly wiring, not new art. Measure the current one before/after:
  `node tools/hud-compare.mjs` shoots the HUD at 390 and prints every control's box.
  **Needs Aaron's eye before it merges** — it changes what the screen looks like
  mid-game, which is the standing comparison rule.

**The Daily Five** (built, NOT merged — Aaron: *"I might make some changes to the
daily 5 so hold"*)
- [x] ~~**Daily Five check harness + break-it proof + before/after comparison.**~~
  ✅ **DONE 08-04.** `tools/daily-check.mjs` now runs **53** checks, all passing.
  Break-it-on-purpose, five sabotages, all caught: random instead of seeded (3
  failures), every league let back in (201 out-of-scope cards), a miss revealing
  the answer, the ★ removed, the Legendary colour changed. Write-up published
  with real desktop and 390px screenshots, including the menu with the stamp
  hidden as the "before".
  Two admissions kept in the record. The first ★ sabotage **passed** — I edited
  `daily.js` when the star lives in `index.html`, so nothing was broken (the
  §1.2d trap, again). And the harness had **only ever measured 1440px**, which is
  how the phone layout below went unseen; `tools/daily-shots.mjs` and five PHONE
  checks now exist.
- [x] ~~**DECISION · the stamp stacks above the title on a phone.**~~
  ✅ **RULED 08-04 — Aaron: "I'm fine with the location of the stamp in mobile."**
  Left as-is; the harness asserts the stacked layout so a future change has to be
  deliberate.
- [x] ~~**DECISION · the share text carries no link to the game.**~~
  ✅ **DONE 08-04.** The receipt now ends with the live address. Hard-coded from
  PLACES.md rather than `location.href` on purpose — the text gets pasted by
  someone who might be on localhost, a preview build, or the old github.io
  address, and every one of those would send their friends somewhere that is not
  the game.
- [x] ~~**Build the missed-card memory.**~~ ✅ **RULED OUT 08-05 — Aaron: *"in
  the daily five we are not doing the cards coming back if you miss because it
  would destroy the everyone gets the same daily five thing."*** Not a deferral
  any more, a decision: the shared daily wins over the make-up rack. Closed.
  The warning written under this item on 08-04 — *"careful, that breaks
  everyone gets the same ten, which is the mode's whole social point"* — is the
  reason it is closed rather than built.

- [ ] **DECISION · does a MISS reveal the answer in the Daily Five?** Aaron
  08-05: *"maybe we can reveal on wrong answers too, but this is to be decided
  just log these thoughts."* Open, and it is open BECAUSE of the ruling above.
  **Why this is now a live question and was not yesterday.** The 22af B5 ruling
  says never show the answer, and its whole justification was *"its scheduled
  return is the second chance."* In the Daily Five the card now never returns —
  so in that mode withholding the answer is not deferred teaching, it is just
  withholding. The premise the ruling rested on stopped being true, which is why
  it is flagged in `design/22af-findings.md` rather than quietly rewritten.
  **The ruling still stands in the main game**, where a card genuinely can come
  back, so this is a per-mode decision, not a reversal.
  Worth weighing when it is decided: a miss is the moment a player most wants to
  know, and the blurb feature above is pointless on a card you got wrong if the
  answer stays hidden. Against it: the taunt (*"I'LL BE BACK."*) is good, and
  people compare daily scores — knowing the answer after a miss changes nothing
  competitively but does remove a reason to think about it.

**Streaks** (built 08-04, Aaron's spec)
- [x] ~~**The streak calendar.**~~ ✅ **DONE 08-04.** A month grid with the
  streak count, reachable from the daily screen itself and from the receipt —
  not only after you finish, because the streak is the reason to come back.
  Three marks, ranked: **gold crown** for all eleven (the ten plus the Heat
  Check), **gold star** for played on the day, **green check** for caught up
  later. Unplayed past days are tappable and playing one **repairs the streak**;
  future days are not. Marks differ in SILHOUETTE, not only colour — crown and
  star are both gold, and this project has already shipped one colour collision.
  Star and crown are drawn as SVG geometry, which is the honest side of the
  medium line: flat vector shapes with clean silhouettes at 14px. If either
  should become a PAINTED object with texture, that is illustration and needs
  sourcing — say so rather than shipping a lumpy approximation.
- [x] ~~**A song for the Daily Five.**~~ ✅ **DONE 08-04.** It was falling
  through to the menu track. Now has its own role, `daily` → Ketsa, "Follow My
  Soul" — one of two tracks Aaron had cast as boombox-only, so no new sourcing.
  One line to swap; a genuinely new song is a sourcing job, not a coding one.

- [x] ~~**The menu stamp shows which mark you earned.**~~ ✅ **DONE 08-04.**
  Aaron asked whether it did. Measured: it did **not** — ordinary day, swept ten
  and all eleven all drew the same green tick. And green had just been given the
  meaning "caught up LATE" on the calendar, so a player who finished today was
  being shown the mark for missing it. Same shape of error as red meaning both
  "worth 3" and "hard". Fixed: **gold star** for playing today, **gold crown**
  for all eleven, and the stamp asks `BKDaily` for both the mark AND the shape,
  so the two screens cannot drift.
  Green check can no longer appear on the stamp, by definition — the stamp only
  reports today, and today can never be "caught up". If Aaron wants a check on
  the stamp it means re-cutting the marks to be score-based rather than
  when-based, which would change what they mean on the calendar too.

**Heat** (shipped)
- [ ] **The heat sound.** This file asked for three things — popup, sound,
  persistent readout — and got two. Aaron 08-03: *"be cool, not essential."*
  Nice-to-have, kept visible so it is not mistaken for done.
- [ ] **Heat phase 2** — streak mode, the heat-check bomb, posterize drain,
  pass/dunk windows. Designed, not built. Not blocking V0.

**Data & trust**
- [x] ~~**Flip the verified-pack gate.**~~ ✅ **FLIPPABLE 08-04.** Aaron: *"flip
  the gate / and then we keep verifying."* The switch is real and it works:
  **`/play/?verified=1`** turns it on for one session (also
  `localStorage.bk_verified_only='1'`, which the harnesses use).
  **It is NOT the default, and that is arithmetic rather than caution.** Measured
  the day he asked: 23 cards survive (nba 15, wnba 8) and five league-tier
  buckets are empty outright — nba t0 and t4, wnba t0/t1/t4, and every evergreen
  card. A game to 11 cannot be dealt from that, and the Daily Five needs a tier-4
  card every single day and there are none.
- [ ] **Make verified-only the DEFAULT.** ⬅ **THE CONDITION IS MET, 2026-08-06.
  The remaining work is one line and one decision, and the decision is Aaron's.**

  Every league × tier pool the game can actually deal from now holds ≥25
  verified cards:

  | | t0 | t1 | t2 | t3 | t4 |
  |---|---|---|---|---|---|
  | **nba** | 26 | 51 | 55 | 41 | 29 |
  | **wnba** | 25 | 32 | 34 | 31 | 25 |

  Journey: pool 23 → **298** shipping cards; deficit **127 → 88 → 47 → 0**.
  (298, not 331: 331 is the `high confidence` count and 33 of those were never
  read. The gate wants both, per DESIGN §10a. Corrected 08-06.)

  **Why "every pool the game can deal from" is the right reading, checked
  rather than assumed.** `build-verified-index.py` still prints THIN POOLS —
  fiba, fives, flags, globetrotters, overseas, street. Every one of those sits
  in a league `LG_LEAGUES` marks `lock:1`. Measured 08-06: **only `nba` and
  `wnba` are unlocked**; big3, flags, overseas, college and street are all
  gated on data and unselectable. So flipping the switch empties nothing a
  player can reach.

  **NOT FLIPPED, on purpose.** `PACKGATE={verifiedOnly:false}` is one line in
  game.js and changes which cards every player sees on the live site. That is
  Aaron's call, not a tidy-up. Two things worth knowing before he makes it:
  - the bank a player draws from gets **smaller and better** — 331 proven cards
    instead of 1,526 mostly-unread ones. Repetition goes up; wrongness goes to
    near zero.
  - it is reversible in one line, and `/play/?verified=1` still previews it for
    a single session without changing anything for anyone else.

  Recount: `python3 tools/build-verified-index.py | grep -E "^\s+(nba|wnba) t"`
  — silence means the condition still holds.

- [x] ~~**FLIP IT.**~~ ✅ **DONE 2026-08-06** — Aaron: *"go ahead and flip the
  verified switch"*. `PACKGATE={verifiedOnly:true}`. The live game now deals
  only cards a human has read against their source.
  Proved in the real game, not from the tables: **`tools/gate-check.mjs`** drives
  the actual picker across every league × decade × tier a player can select —
  **1,080 cards dealt, 0 unverified, 93 checks**. Two-sided: with the gate off
  the same draws produce **195** unverified cards, so the check demonstrably
  bites.

- [ ] **ONE CARD IN EIGHT IGNORES THE DECADE YOU PICKED.** Measured 08-06 by
  gate-check: the era filter was dropped on **125 of 1,080 draws (12%)**. This
  is the documented fallback in `pickQuestionIdx` doing its job — when a
  (league, era, tier) pool is empty it falls to league-neutral + era, then to
  league-neutral with the era check dropped, rather than crashing. Nothing is
  broken and no unverified card gets through.
  But a player who picks the 1970s and gets a 2020s card will notice, and it got
  more likely the moment the gate turned on, because the pool shrank from 1,526
  to 298. The fix is not code — it is **more verified cards in the thin
  decades**. Worst pools, verified count: nba-1940s 2, nba-1950s 2, nba-1970s 8,
  wnba-1990s 12. Recount with the era table in the 08-06 commit.
- [x] ~~**Move cards onto fact numbers.**~~ ✅ **DONE 08-04. R1: 829 → 0.**
  It was never research. R1 asked whether a card's `src` string happened to BE a
  fact id, and it never was for any card — the emitter simply never wrote the
  link. Cards now carry `f` (the fact id) alongside `src` (the source url, kept
  for the record). Nothing player-facing changed: **the game never read `src`**,
  measured — every `.src` in game.js is an image.
  What it unlocked: cards can finally inherit a fact's proof. Cards that would
  ship if the gate flipped went **1 → 23** on the back of the 24 facts verified
  the same day.

**Research runs Aaron asked for and never got**
- [ ] **22af Runs D and E.** Aaron asked for both on 08-03. D is the player
  wishlist; E is lanes 3 and 5. Build the brief with the `design-research-brief`
  skill — these study other GAMES, not basketball facts. **No public "no one else
  has built this" claim is allowed until E lands**, because Run C already refuted
  the moat as originally worded. Findings go to `design/22af-findings.md`, the
  existing home — not a new file.

**Before it goes to the twenty**
- [ ] **The full list Aaron asked for.** Aaron 08-03: *"I wanna see a full list
  that I said I want before getting it out to the twenty friends."* Generate it
  with the `status-board` skill — that is the one format — not a fresh invention.

## Where the detail lives

- Full reasoning per item: `BUILD.md`, entry **V0 · LOCKED BY AARON**
- The tickable board: https://claude.ai/code/artifact/99f89fb6-7861-4573-9850-b4ef81eaf506
- How to work here: `CLAUDE.md` · what went wrong before: `MAKING.md`

**Keep this file current.** When something ships, tick it here first. A plan
nobody updates becomes the July 29 board.

---

## Reconciliation log

**2026-08-04** — the source register shipped; open items filed for the first time.

- **R3 is done**, 513 → 0. The only run to move since this file was locked.
- **This file had two counting errors**, both now marked in place rather than
  silently corrected: "all 9 research runs" over a table of 8 (there is no R4),
  and "the build — 23 items" over a list of 22.
- **Eleven open items were living only in chat replies** and are now written down
  above and in RESEARCH-BACKLOG V13–V18. Aaron: *"every time you come up with
  something that still needs to be done... make sure it does not get lost or
  forgotten."* `tools/open-items.py` + the `open-items` skill are the mechanism.

**2026-08-03** — checked every tick against the repo, not memory.

- **No data run has moved.** R1–R8 counts identical to the day this file was
  locked. `todo_open` 4,043.
- **The 22af findings were already committed**, at `design/22af-findings.md`, not
  at `docs/play/data/research-22af.md`. No second copy was created: CLAUDE.md
  forbids parallel notes files, and one home per thing is the rule that keeps this
  repo honest. If the data folder is the preferred home, the file should MOVE, not
  be duplicated.
- **The verified-pack gate exists and is dark.** That is the designed state, not
  an oversight.
- **Heat shipped without its sound.** This file asked for three things and got two.
- **"Heat multiplier" was stale** — corrected to abilities, per the 08-02 DESIGN
  lock made a day after this file.
- **The Daily Five jumped the queue** (item 10, built ahead of 5–9).
- Everything in items 7, 8, 9, 11 and 12 is untouched.
